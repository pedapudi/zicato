"""Dashboard support modules for zicato.

Pure, server-agnostic helpers that turn raw run telemetry into shapes the
live dashboard renders. The dashboard server imports from here; nothing
in this package performs network I/O or model calls.
"""
