# Line budgets

Line count is a deliberately blunt proxy for structural simplicity. The
simplification program constrains tracked non-documentation lines, the
production subset of them, and the executable lines inside that subset, so
neither deleting tests nor deleting prose can stand in for reducing logic.
Detailed measurement and verification instructions live in
[`docs/dev-guide/11-testing.md`](../dev-guide/11-testing.md#line-budget-gate).

## Measurement contract

Three counts are taken over the tracked files, and each carries its own
ceiling.

**Total** counts newline characters in tracked files, excluding Markdown,
dependency lockfiles, and the paths `EXCLUDED_FROM_BUDGET` in
`tools/line_budget.py` names, each with the reason it holds no implementation
a simplification could reach.

**Production** is the subset of those files under the runtime package, crate
source directories, integrations, and the build hook; tests and assets stay
outside that subtotal.

**Production logic** counts, over those same production files, only the lines
that execute: for Python, a line that is neither blank, nor a comment, nor part
of a docstring; for JavaScript, a line that is neither blank nor comment-only;
for Rust, a line that is neither blank, nor comment-only, nor part of an item
carrying `#[cfg(test)]`; for every other file type, the raw newline count,
because the tool holds no comment syntax for them. A comment or docstring
sharing a line with code leaves that line executable.

The logic row's two numbers are what this definition yields. A
`production_logic` value named in an appendix entry below is measured over a
definition reaching only Python and JavaScript. Each such value is higher by
the supervisor crate's comment and test lines — 6,748 of them at the baseline
reference and 6,632 at the enforced limit. Extending the definition to a
language is not a deliberate increase, so both the baseline and the limit move
to what the definition measures, and the appendix carries no row for that move.

Documentation and comments therefore reach the total and the production
subtotal and never the logic count. Writing them spends two budgets and
deleting them relieves neither ceiling that measures logic, so the third
ceiling can only be moved by removing code. The checker owns the exact
classifications.

The baseline and the enforced limit use the same metric within each row. All
three limits below are the ones `.line-budget.json` holds, and the last column
subtracts the baseline from the limit: it is positive where the limit stands
above the baseline and negative where it stands below.

| Measurement | Baseline (`f9052dd`) | Enforced limit | Limit minus baseline |
|---|---:|---:|---:|
| Total | 408,661 | 471,206 | +62,545 |
| Production | 197,702 | 204,264 | +6,562 |
| Production logic | 110,276 | 113,518 | +3,242 |

The baseline row is the reference `f9052dd` measured by the classification the
checker holds, which counts the console's hand-written entry point
`src/zicato/dashboard/static/console.js`, which carried the name `app_T.js` at
that reference and 114 lines. A
provenance record of 408,547 total and 197,588 production for the same
reference is that measurement taken while the exclusion list also named that
file. The raw count of 425,755 retained in `.line-budget.json` includes
lockfiles and excluded paths and is not an enforced metric.

## Production logic by subsystem

The three counts are also taken per subsystem, where a subsystem is a package
directly under `src/zicato/`, a crate, an integration, or a top-level path, and
a subsystem's three numbers partition the repository-wide ones: the production
logic column below sums to the enforced logic count. Prose share is the share
of a subsystem's production lines that do not execute (blank lines, comments,
and docstrings), `1 - logic / production`. CSS and HTML hold no logic counter,
so every line of the console's stylesheet counts as executable here as in the
enforced measurement.

The table lists every subsystem holding production files in descending order
of production logic and states the working tree of the commit that wrote it.
`python tools/line_budget.py --write-summary` rewrites it from a fresh
measurement, and `--check-ledger` fails when the table differs from the tree it
runs in, so a change to production code lands with the table it produces.
`--report` prints the same table for every subsystem, and `--history` prints the
production-logic series per subsystem along a branch's first-parent commits.

| Subsystem | Total | Production | Production logic | Prose share |
|---|---:|---:|---:|---:|
| src/zicato/dashboard | 72,986 | 39,291 | 27,204 | 30.8% |
| src/zicato/query | 18,672 | 18,672 | 11,159 | 40.2% |
| src/zicato/evolve | 12,662 | 12,662 | 7,533 | 40.5% |
| src/zicato/reflection | 9,779 | 9,779 | 6,159 | 37.0% |
| src/zicato/epoch | 12,287 | 12,287 | 5,704 | 53.6% |
| crates/supervisor | 14,561 | 12,130 | 5,498 | 54.7% |
| src/zicato/tournament | 10,793 | 10,793 | 5,498 | 49.1% |
| src/zicato/proposer | 11,053 | 11,053 | 5,491 | 50.3% |
| src/zicato/cli | 8,192 | 8,192 | 5,178 | 36.8% |
| src/zicato/analyzer | 7,635 | 7,635 | 4,781 | 37.4% |
| src/zicato/selection | 5,271 | 5,271 | 2,837 | 46.2% |
| src/zicato/index | 5,513 | 5,513 | 2,773 | 49.7% |
| src/zicato/tui | 3,839 | 3,839 | 2,535 | 34.0% |
| src/zicato/telemetry | 4,684 | 4,684 | 2,236 | 52.3% |
| src/zicato/runtime | 5,080 | 5,080 | 2,183 | 57.0% |
| src/zicato/core | 6,274 | 6,274 | 1,970 | 68.6% |
| src/zicato/contract_draft | 2,928 | 2,928 | 1,684 | 42.5% |
| src/zicato/health | 2,268 | 2,268 | 1,226 | 45.9% |
| src/zicato/builder | 2,165 | 2,165 | 1,222 | 43.6% |
| src/zicato/mutation | 2,607 | 2,607 | 1,135 | 56.5% |
| src/zicato/board | 2,346 | 2,346 | 1,044 | 55.5% |
| src/zicato/check | 1,566 | 1,566 | 956 | 39.0% |
| src/zicato/adapters | 2,168 | 2,168 | 793 | 63.4% |
| src/zicato/testing | 1,447 | 1,447 | 760 | 47.5% |
| src/zicato/judge_runtime | 1,749 | 1,749 | 740 | 57.7% |
| src/zicato/_tournament_worker.py | 1,262 | 1,262 | 633 | 49.8% |
| src/zicato/workspace | 1,614 | 1,614 | 629 | 61.0% |
| src/zicato/synthetic | 1,139 | 1,139 | 544 | 52.2% |
| src/zicato/scoring | 1,388 | 1,388 | 464 | 66.6% |
| src/zicato/patterns | 804 | 804 | 417 | 48.1% |
| src/zicato/models_config.py | 447 | 447 | 356 | 20.4% |
| src/zicato/emulator | 675 | 675 | 306 | 54.7% |
| src/zicato/example_workspace | 658 | 658 | 301 | 54.3% |
| src/zicato/storage | 931 | 931 | 289 | 69.0% |
| src/zicato/config.py | 679 | 679 | 225 | 66.9% |
| src/zicato/logging_stream.py | 445 | 445 | 215 | 51.7% |
| src/zicato/runtime_factory.py | 421 | 421 | 210 | 50.1% |
| src/zicato/workspace_loader.py | 362 | 362 | 143 | 60.5% |
| src/zicato/integrations | 143 | 143 | 100 | 30.1% |
| src/zicato/reasoning.py | 112 | 112 | 83 | 25.9% |
| src/zicato/adapter_factory.py | 218 | 218 | 73 | 66.5% |
| src/zicato/import_path.py | 155 | 155 | 60 | 61.3% |
| hatch_build.py | 97 | 97 | 54 | 44.3% |
| src/zicato/util | 173 | 173 | 50 | 71.1% |
| src/zicato/__init__.py | 74 | 74 | 48 | 35.1% |
| src/zicato/orchestrator.py | 14 | 14 | 11 | 21.4% |
| src/zicato/aux_timeout.py | 54 | 54 | 8 | 85.2% |

## Ratchet policy

There is no temporary allowance. A change exceeding any of the three limits
fails. A deliberate increase must update the limit and record the previous
value, signed delta, new value, issue, and reason in this document. Reductions
ratchet each machine limit directly to the new measured total.

Minification, concatenation, moving implementation into excluded paths,
checked-in generated replacements, weakening tests, or deleting documentation
and comments do not qualify as simplification. Any classification change
receives the same review as a budget increase; where it corrects an exclusion
and so brings real source into a measurement, the ceiling rises by the lines the
correction exposes and the entry records that reason.

The appendix below is append-only, and `python tools/line_budget.py
--check-ledger` enforces it. Read from the table alone, every row's previous
value plus its signed delta must equal its new value, each row must start no
higher than the value the preceding row for the same measurement reached, and
the last row for a measurement must stand at or above that measurement's
enforced limit, because an increase ends at the limit it sets and a reduction
carries the limit further down with no row. Given a base revision (`--base
origin/main`), every row that revision records must still be present with the
same label, measurement, and numbers, while a reason is free to be reworded.
The line-budget job in CI runs the check against the pull request's base
branch, so a merge that resolves the table toward one side fails with the
dropped rows named.

## Deliberate increases

| Change | Previous | Delta | New | Reason |
|---|---:|---:|---:|---|
| Durable run-artifact capture (total) | 407,445 | +399 | 407,844 | Issue #12: deterministic inventory, persistence, grading contract, and regression coverage for emitted files. |
| Durable run-artifact capture (production) | 196,526 | +235 | 196,761 | Issue #12: bounded capture implementation and typed artifact surface. |
| Harmonograf web-port readiness (total) | 407,791 | +21 | 407,812 | Launch handle must not be returned before its web listener accepts; fixes the alternating connection-refused e2e failures. |
| Harmonograf web-port readiness (production) | 196,361 | +21 | 196,382 | Bounded accept-poll in the launcher; degrades to the JSONL-only handle on timeout. |
| Harmonograf readiness hardening (total) | 407,812 | +67 | 407,879 | Timeout path now stops the launched server before returning the no-op handle; /healthz replaces the TCP probe; two deterministic timeout regressions. |
| Harmonograf readiness hardening (production) | 196,382 | +6 | 196,388 | Handle-first construction and shutdown-on-timeout in the launcher. |
| Execution-tree stated statuses and delegation nesting (total) | 407,860 | +158 | 408,018 | Statuses from invocation boundary-exit and cancel events; delegation observations nest under the delegating invocation's stated id; regression coverage for deep agent/tool mixtures. |
| Execution-tree stated statuses and delegation nesting (production) | 196,369 | +51 | 196,420 | Boundary-exit and cancel status handling plus the explicit delegation parent edge in the transcript reader. |
| Execution-tree error-visibility regressions (total) | 408,018 | +32 | 408,050 | Renderer tests pinning failed/cancelled styling on branch, leaf, and tool nodes and live error repaint of the run rail; the accompanying recursive rail signature reduced production by 2 (ratcheted to 196,418). |
| Lexical static-file guard (total) | 408,050 | +86 | 408,136 | Issue #231: first coverage of the static guard — a traversal-refusal test and a symlink-staged bundle test — plus non-emptiness floors on the package-tree walks the structural pins depend on. |
| Lexical static-file guard (production) | 196,418 | +16 | 196,434 | Issue #231: lexical normalize-and-reject in `_serve_static` and the unresolved-first relative path in `_rel_file`. |
| Parity macOS bash 3.2 compatibility (total) | 408,136 | +2 | 408,138 | Empty-array-safe expansions plus a glob-safe comma-list split in tools/parity.sh; the ladder now runs on a stock macOS shell. |
| Pi shipped-asset root (total) | 408,138 | +12 | 408,150 | Issue #238: the recurrence guard — a test pinning that the launcher leaves shipped-asset resolution to pi, and the live envelope lane rebuilt through the real env builder. |
| Pi shipped-asset root (production) | 196,434 | +5 | 196,439 | Issue #238: the env-builder docstring stating which root is process state, so the variable is not re-added. |
| Proposer input capture (total) | 408,150 | +750 | 408,900 | Issue #244: the locked append-only writer and tolerant reader, four capture sites, and the concurrency, retry, and degrade regressions. |
| Proposer input capture (production) | 196,439 | +286 | 196,725 | Issue #244: the capture module, per-site wiring, the slot coordinate, and the path accessors. |
| Proposer baseline losses and metric priorities (total) | 408,900 | +1,013 | 409,913 | Issues #243 and #247: the shared reserved-base filter and its allow-list regression, the calibration-band fallback and its degraded-probe and holdout regressions, the priority renderer, and the contract-priority suite. |
| Proposer baseline losses and metric priorities (production) | 196,725 | +587 | 197,312 | Issues #243 and #247: the own-code draw filter, the folded calibration read, the priority resolver and renderer, the calibration span guard, and the per-site wiring of the rendered block. |
| Unit provenance (total) | 409,913 | +642 | 410,555 | Issues #242 + #245: attempt-slot records, the wall-clock span, the attributed not-completed penalty, and the provenance regression module. |
| Unit provenance (production) | 197,312 | +163 | 197,475 | Issues #242 + #245: the three OUTPUT-only loss fields, worker stamping, attempt recording, and the carried-champion attempt guard. |
| Patch diff against the recorded parent (total) | 410,555 | +981 | 411,536 | Issue #253: the lineage-resolved baseline with a pickable base and context expansion, plus the truncation, per-column-room, record-vs-tree, and reconstruction-caption regressions. |
| Patch diff against the recorded parent (production) | 197,475 | +383 | 197,858 | Issue #253: the diff view's baseline resolution, the base picker, the expansion machinery, and the reconstructed_against flag on mutation detail. |
| Per-entry outcomes on the configured signal (total) | 411,536 | +1,073 | 412,609 | Issue #246: the served decided_by resolver, the shared replicate-score enumeration, the six-surface client sweep, and the signal regressions. |
| Per-entry outcomes on the configured signal (production) | 197,858 | +530 | 198,388 | Issue #246: delta_score/score_se/drift_present on the matchup grid and per-entry readers plus the channel-aware figure. |
| Pre-spend workspace gate (total) | 412,609 | +2,297 | 414,906 | Issue #240: the check package (shared workspace view, validators, report), the gate at both spend boundaries, the structural unbound-span-marker collector, the reconstructible stub adapter the parity capture now runs the gate against, and the severity, probe-environment, probe-timeout, model-role, and advisory-tier regressions. |
| Pre-spend workspace gate (production) | 198,388 | +1,240 | 199,628 | Issue #240: the validators and their lazily-built workspace view, the shared `duplicate_mutation_ids` helper, `make_adapter_from_spec`, the process-group-bounded adapter probe, and the enumerator's unbound-marker collector. |
| Replicate-keyed run identity (total) | 414,906 | +727 | 415,633 | Issue #250: replicate-suffixed run ids and events files, the single-producer args payload, the per-replicate transcript readers, and the collision/telemetry pins. |
| Replicate-keyed run identity (production) | 199,628 | +310 | 199,938 | Issue #250: the legible reserved-prefix id, per-replicate sink paths, and the any_unit_transcript reader shared by the three proposer-channel consumers. |
| Execution-plan reader (total) | 415,633 | +1,793 | 417,426 | Issue #241 (partial): the epoch execution-plan builder, its endpoint, the indexed replicate walk, and the plan regressions incl. the on-disk audit. |
| Execution-plan reader (production) | 199,938 | +1,091 | 201,029 | Issue #241 (partial): query/execution_plan.py, the endpoint registration, and the shared indexed enumerator. |
| Replicate-slot overlap (total) | 417,426 | +860 | 418,286 | Issue #251: entry-chained slot overlap behind the no-budget predicate, the shared scorer, and the deterministic utilisation/Rule-B/budget-path regressions. |
| Replicate-slot overlap (production) | 201,029 | +378 | 201,407 | Issue #251: the overlap predicate, entry chains, the two path wrappers, and the fast-mode shared semaphore. |
| Fresh in-flight tally (total) | 418,286 | +74 | 418,360 | Issue #268: the shared fresh_run_count helper, the aged pipeline tally, and the two-reader agreement pins. |
| Fresh in-flight tally (production) | 201,407 | +20 | 201,427 | Issue #268: fresh_run_count in runtime_view, shared by derive_liveness and the round pipeline. |
| Dry-run reachability probe (total) | 418,360 | +506 | 418,866 | Issue #264: the probe module, its dry-run wiring, and the stub-driven per-role coverage including the keyless-spec and bounded-timeout pins. |
| Dry-run reachability probe (production) | 201,427 | +221 | 201,648 | Issue #264: the per-role bounded round trip through the worker's construction seam, the per-role report renderer, and the dry-run report-and-exit wiring. |
| Measurement bands on the execution plan (total) | 418,866 | +661 | 419,527 | Issue #241: the band steps with their attribution anchors, the shared complement walk, and the set-based coverage audit. |
| Measurement bands on the execution plan (production) | 201,648 | +453 | 202,101 | Issue #241: measurement_band/measurement_draw nodes, the screen-round anchor, and the partitioned enumerations. |
| Process-identity reaping of in-flight records (total) | 419,527 | +210 | 419,737 | Issue #270: the host-locality predicate, the identity gate in fresh_run_count, and the dead-child / off-host / no-identity-fields regressions. |
| Process-identity reaping of in-flight records (production) | 202,101 | +77 | 202,178 | Issue #270: the lock-derived host-locality proof and the per-record identity check the tally composes with the staleness window. |
| Replicate-aware judge repair (total) | 419,737 | +234 | 419,971 | Issue #265: the shared slot walk with its records/evidence split, per-slot transcript pairing, and the idempotence and outcome-count regressions. |
| Replicate-aware judge repair (production) | 202,178 | +93 | 202,271 | Issue #265: persisted_loss_slots beside the evidence walk and the per-slot repair loop with honest outcome counts. |
| Calibration phase and progress (total) | 419,971 | +439 | 420,410 | Issue #175: the calibrating phase with draw progress, the epoch-open pipeline step, the cost line, and the phase/render regressions. |
| Calibration phase and progress (production) | 202,271 | +150 | 202,421 | Issue #175: the beater threading, the on_draw seam, the epoch-open projection, and its two renderers. |
| De-centered scalar channels (total) | 420,410 | +624 | 421,034 | Issue #262: the derived judge/failure/runtime channels, the identity and refusal pins, and the seam test's rewritten second implementation. |
| De-centered scalar channels (production) | 202,421 | +121 | 202,542 | Issue #262: the two failure-channel contract fields with their load-time invariant, the channel derivation on LossProfile, the within-channel resolver, and the replicate fold of the run-outcome fields — net of the deleted drift_weight/runtime_weight fields, the not-completed penalty helper, the reducer's dead kind-multiplier, and the runner's inline abort arithmetic. |
| Per-epoch champion pointer in the tree (total) | 421,034 | +141 | 421,175 | Issue #280: the multi-epoch `buildTreeModel` crown regressions (each epoch crowns its own champion; a pointerless epoch stamps neither flag), the per-epoch scoped read, and the memoized closed-epoch pointer. |
| Per-epoch champion pointer in the tree (production) | 202,542 | +47 | 202,589 | Issue #280: the `?epoch=`-scoped champion read per epoch node, replacing the single bare-read pointer gated on the contract epoch, plus the data-layer `closedEpochChampion` memo that keeps a closed epoch off the live-bust re-fan. |
| Field-round champion from the role tag (total) | 421,175 | +530 | 421,705 | Issue #284: the tagged-champion resolver with its fallback chain, the nullable eval-mode provenance, and the five round-timeline regressions. |
| Field-round champion from the role tag (production) | 202,589 | +75 | 202,664 | Issue #284: _field_champion reading the recorded role, the per-round metadata borrow, and the relocated NULL-means-full default. |
| The seed is not a round (total) | 421,705 | +133 | 421,838 | Issue #286: the parentage-tested seed skip on both sides, the re-armed malformed-stamp pin, and the phantom-round regressions. |
| The seed is not a round (production) | 202,664 | +21 | 202,685 | Issue #286: the writer's parentless skip and the reader's parentage guard. |
| Critic choice and rationale on the round log (total) | 421,838 | +419 | 422,257 | Issue #279: the two-line critic ask with its mutation-proofed prompt pin, the slate summary on both transports, the missing-rationale bind, and the restored payload pin. |
| Critic choice and rationale on the round log (production) | 202,685 | +133 | 202,818 | Issue #279: the rationale-threading parse and emit path shared by both transports, and the simplified pi wrapper. |
| One-classifier seed decision (total) | 422,257 | +24 | 422,281 | Issue #291: the cross-feed probe pinning that the epoch and lineage payloads serve the identical (promoted, decision, decision_label) triple for every generation, seed included. |
| One-classifier seed decision (production) | 202,818 | +3 | 202,821 | Issue #291: the copy-off-the-lineage-node invariant comment at the recompute site that produced the disagreement, net of the deleted local derivation and of the shortened best-of-N mount docstrings. |
| Pre-flight phase and stable delegation pin (total) | 422,281 | +452 | 422,733 | Issues #276 + #260: the pre-flight phase token with combined probe progress, the epoch-open step table, and the deterministic reaping pin with its load-tolerant window. |
| Pre-flight phase and stable delegation pin (production) | 202,821 | +139 | 202,960 | Issues #276 + #260: the beater threading with restore-on-refusal, probe_selection_bounds, and the generalized epoch-open projection. |
| Live execution plan and the served per-run in-flight verdict (total) | 422,733 | +1,218 | 423,951 | Issue #241: the live plan builder with its liveness gate, active-path projection and run-scope placement, the per-row `fresh` verdict shared with the tally, the client's consumption of it with its absent-field fallback, and the 23 plan / 4 verdict / 4 node regressions plus the re-captured parity snapshot. |
| Live execution plan and the served per-run in-flight verdict (production) | 202,960 | +626 | 203,586 | Issue #241: query/live_execution_plan.py, its endpoint, route and payload contract, and the per-record predicate `fresh_run_count` and `read_active_runs_view` now share. |
| Recorded promoted head and the collapsed slate's selection (total) | 423,951 | +645 | 424,596 | Issues #287 + #292: the one promoted-head reader with its source order, the sole-candidate selection event, and the multi-promote, byte-identity, and pointer regressions. |
| Recorded promoted head and the collapsed slate's selection (production) | 203,586 | +224 | 203,810 | Issues #287 + #292: promoted_head.py, the gate/spine/champion consumers, and the shared-builder emission on the collapsed slate. |
| Gauntlet and fast-mode golden lanes (total) | 423,921 | +5,651 | 429,572 | Issue #310: three additional mock-evolve captures (gauntlet full, gauntlet fast, racing fast) and the round-log plus field-tournament sections now captured in all four, which together are 4,474 of the delta; plus the source-backend configuration boundary — its store guard, the repair command, the degrade paths in the two dashboard readers, and their regressions. The captures are the coverage: before them the unified round's single-challenger branches and the whole of fast mode executed in no golden. |
| Gauntlet and fast-mode golden lanes (production) | 202,884 | +502 | 203,386 | Issue #310: the knob-versus-disk guard and its evidence readers in genstore.py, `zicato repair generation-source-backend`, the `--reset-lineage` gate on `init --force`, the store-optional degrade in dashboard/filetree.py and dashboard/mutations.py, the one shipped `ReplicateDuel` implementation lifted into selection/driver.py, and the restored fast-mode asymmetry warning. |
| Exact scalar aggregation (total) | 429,572 | +205 | 429,777 | Issue #310: the six-case exactness suite with its two witnesses, the `math.fsum` conversions and their stated invariant, the interpreter-independent reference implementations in the scoring-seam suite, and the parity job's Python matrix. |
| Exact scalar aggregation (production) | 203,386 | +36 | 203,422 | Issue #310: `math.fsum` in place of the builtin sum and of running float accumulators across the seven modules on the served-scalar path, plus the invariant stated in the aggregation module's docstring. |
| Multi-round and non-racing golden lanes (total) | 429,777 | +7,773 | 437,550 | Issue #316: four additional mock-evolve captures — a two-round racing run and one each for the swiss, single-elimination, and double-elimination structures — which are 7,506 of the delta; plus their three contract variants, the lane table's round-count axis with its persisted carry-over assertions, and the four gates. The captures are the coverage: before them no golden ran a second round against a crowned parent, and the three non-racing registries executed in no golden at all. |
| Scoped round-log events (total) | 437,550 | +3,926 | 441,476 | Issue #307 step 1: 3,161 of the delta is the eight mock-evolve goldens, which gain a `scope` object per round-log record and lose no line; the remaining 765 are the `RoundEventScope` envelope with its coordinate vocabulary and grouping key, the emitter's third scope argument across the propose and duel call sites, the two registry-correspondence tests holding the step table equal to the plan's, and the round-trip, forward-compatibility, slate-scope and duel-wiring regressions. |
| Scoped round-log events (production) | 203,422 | +380 | 203,802 | Issue #307 step 1: scope serialization, decoding and attribute promotion in `round_log.py`, the scope argument on `_RoundLogEmitter.emit` / `_emit_tournament_units` / `_emit_gate_evaluated`, the shared `_duel_scope` builder, the declared stepless-token set, and the field, persist and best-of-N call sites. |
| Prose lint for hidden-context constructions (total) | 441,476 | +558 | 442,034 | The dependency-free checker over the documentation, README, CHANGELOG, runtime, example, skill, and tool trees; its per-rule fixture suite; the committed per-rule baseline; and the ratchet job in CI. Production is unchanged: the tool sits outside the runtime package. |
| Temporal hedges and the changelog exemption (total) | 441,975 | +46 | 442,021 | The seventh prose rule with its per-phrase and severity coverage, the per-file rule-exemption table with its changelog regression, and the seventh entry in the committed baseline. Production is unchanged: the checker sits outside the runtime package. |
| Reader parity over every workspace reader (total) | 442,021 | +11,341 | 453,362 | Issue #324: the recorded golden is 9,869 of the delta — 88 labelled reader outputs over a fixture with two epochs, eleven generations, per-run replicates, two board reflections, three round logs and a derived index; the remaining 1,472 are the fixture builder, the capture, and the ordering and reproducibility gates. The golden is the coverage: before it the analyzer, reflection, health, index, workspace and CLI readers had no pinned output at all, so any of them could change what it returns without a test noticing. Production is unchanged: the change adds only tests and fixtures. |
| Executable-line measurement (total) | 453,362 | +173 | 453,535 | Issue #324: the per-language logic counters in `tools/line_budget.py`, the third enforced ceiling, the stated reason beside each excluded path, and the docstring, comment, and block-comment fixtures that pin what the logic count drops. |
| The console entry point counted (total) | 453,535 | +113 | 453,648 | Issue #324: `src/zicato/dashboard/static/console.js` is hand-written source, so it is absent from the exclusion list and its lines are measured like the rest of the console. |
| The console entry point counted (production) | 203,751 | +113 | 203,864 | Issue #324: the same correction, in the subtotal the file belongs to as runtime source under `src/zicato/`. |
| Contract knob bounds declared once (total) | 453,648 | +309 | 453,957 | Issue #324: the shared constraint type with its finiteness, range and closed-vocabulary checks; the bound declared on each of the twenty-four contract knobs that has one; the `replicates` bound on the otherwise opaque tournament-structure params; and the guard pairing every declared bound against both the contract loader and the builder operation the knob's own metadata names. Two knobs gained a bound they never had: `promote_margin`, where a negative value inverted the promote gate into promoting a regression, and `replicates`, which every selection strategy silently clamped up to one. |
| Contract knob bounds declared once (production) | 203,864 | +167 | 204,031 | Issue #324: `core/constraints.py`, the declarations on the field definitions, and the params check in `TournamentStructure`, against which the builder's twelve private copies of the same rules are removed. |
| Contract knob bounds declared once (production logic) | 121,581 | +46 | 121,627 | Issue #324: the net across four files. `core/constraints.py` adds 51 executable lines — the constraint dataclass, its check, the finiteness helpers moved out of `core/scoring_config.py`, and the two lookups the builder calls. `builder/operations.py` loses 18, the twelve inline range and vocabulary checks replaced by one call each. `core/scoring_config.py` nets +6: about twenty range and finiteness statements leave its four `__post_init__` bodies, and the field declarations that now carry a bound wrap onto more lines than they did. `core/tournament.py` adds 7 for the structure-params bound table and the loop that applies it. |
| Generation ordering by round number (total) | 453,957 | +128 | 454,085 | Issue #324: the eleven-generation regression suite over every generation enumeration — the analysis pass, the two dashboard views, the health command, the per-round health inputs, the two resolvers of an epoch's current generation, and the agreement of the two id minters. Eleven is the smallest count at which lexical and round-number order differ, so no shorter fixture can catch the defect. Executable production lines fall by 34: one round-number parser replaces the ten hand-rolled ones, one ordering key replaces six, and one minter replaces two, ratcheting production logic to 121,593 and production to 204,002. |
| Recorded effective settings (total) | 454,085 | +429 | 454,514 | Issue #309: the settings record — the source vocabulary, the map of every effective setting to its value and the tier that set it, the additive `settings` field on the heartbeat, and the per-tier, attribution and record-completeness coverage. Before it, a run's effective configuration was reconstructible only by re-deriving the composition by hand, and a concurrency ceiling nobody wrote down was indistinguishable from one an operator chose. |
| Recorded effective settings (production) | 204,002 | +232 | 204,234 | Issue #309: `runtime/effective_settings.py`, the `settings` field with its serialization and tolerant read, the beater argument that carries it forward, the stamp where the round resolves its runtime configuration, and `resolve_host_worker_permits` in the factory. |
| Recorded effective settings (production logic) | 121,593 | +124 | 121,717 | Issue #309: the net across six files. `runtime/effective_settings.py` adds 91, of which 33 are the two declared tables — the seventeen runtime knobs the record reports and the sixteen fields it excuses with a reason. `runtime_factory.py` adds 11 for `resolve_host_worker_permits`, which replaces the bool-intent mapping that `make_runtime_config` and the run-start log line each held a copy of; `evolve/loop.py` loses 1 as its copy goes. `runtime/state.py` adds 8, `runtime/heartbeat.py` 5, and `evolve/round_entry.py` 10 for the best-effort stamp. |
| Practice-review keys the console reads (total) | 454,514 | +190 | 454,704 | Issue #324: the terminal console's practice table rendered two empty columns because it read key names the reflection endpoint does not serve, and the fixture that should have caught it stated the same wrong names by hand. The delta is the coverage that makes the fix durable: the fixture rebuilt from `PracticeReview.to_json`, three lens tests over a real `PracticeCheck`, and a per-check key cross-check holding the serializer, the live service and the fixture equal — reflection payloads were exempt from the existing top-level cross-check, so no test read inside them. |
| Practice-review keys the console reads (production) | 204,234 | +10 | 204,244 | Issue #324: the practice row now reads `check_id`, `headline` and `rationale`, states an unmeasured check's missing input in its evidence slots, and folds what the row displays into the repaint digest. Most of the delta is the comment stating which serializer owns the key names. |
| Practice-review keys the console reads (production logic) | 121,717 | +2 | 121,719 | Issue #324: the repaint digest folds four fields per check instead of two, and the row builder binds the check's id and headline once rather than re-reading them. |
| Holdout held back in the fast-mode gauntlet (total) | 454,704 | +214 | 454,918 | Issue #319: the five-case regression module — a holdout-only improvement refused, a train win confirmed on the holdout and crowned, a memorized win flipped, the full-mode control the fast round must agree with, and the empty-holdout degrade. Three of the five fail against the code this replaces. Production falls by 9 and production logic by 9: the whole-board branch of the selector and the parameter that disabled the crowning confirmation are removed, and both machine limits ratchet down to the measured totals. |
| Rejected-round summary pinned to the gate (total) | 454,579 | +81 | 454,660 | Issue #10: a rejected round's summary must state the same champion and challenger scalars its rejection reason states. The delta is the pin — one test per non-gauntlet structure that reads both numbers out of the gate's own sentence — plus the `structure` keyword on the multi-challenger fixture that lets one test body run against all four. Production is unchanged: the summary already sourced those scalars correctly, and nothing but tests changed. |
| Empty and absent read as one value in the divergence auditor (total) | 454,501 | +82 | 454,583 | Issue #61: the supervisor's auditor compared a field's two spellings — written empty by the older writers, written null by the current ones — and reported a legacy workspace as divergent from its own index. The delta is the guard that collapses them, the comment stating which comparisons use it and which read the raw value, and three unit tests: the parent case, the contract-hash case, and a genuine parent disagreement that must still be flagged. The same 82 lines land in all three counts, because that `production_logic` value is measured over a definition reaching only Python and JavaScript. |
| Empty and absent read as one value in the divergence auditor (production) | 204,003 | +82 | 204,085 | Issue #61: the supervisor's auditor compared a field's two spellings — written empty by the older writers, written null by the current ones — and reported a legacy workspace as divergent from its own index. The delta is the guard that collapses them, the comment stating which comparisons use it and which read the raw value, and three unit tests: the parent case, the contract-hash case, and a genuine parent disagreement that must still be flagged. The same 82 lines land in all three counts, because that `production_logic` value is measured over a definition reaching only Python and JavaScript. |
| Empty and absent read as one value in the divergence auditor (production logic) | 121,379 | +82 | 121,461 | Issue #61: the supervisor's auditor compared a field's two spellings — written empty by the older writers, written null by the current ones — and reported a legacy workspace as divergent from its own index. The delta is the guard that collapses them, the comment stating which comparisons use it and which read the raw value, and three unit tests: the parent case, the contract-hash case, and a genuine parent disagreement that must still be flagged. The same 82 lines land in all three counts, because that `production_logic` value is measured over a definition reaching only Python and JavaScript. |
| Dashboard read routes driven from one table (total) | 453,014 | +2,895 | 455,909 | Issue #324: fifty dashboard read routes were the same handler written fifty times, and are now fifty rows of one declarative table plus one factory that builds them; two further routes join it — the effective-settings read (#309) and the live pipeline, now a projection of the same read that serves the live execution plan (#241). Production falls by 22 and production logic by 49: the collapse removes 640 lines of handler bodies and the table's rows put most of them back as data, because a row carries what each route serves and how it degrades — the information the deleted docstrings held. The total rises by 2,895 for the response snapshot: one recorded status and body per table route over the standard fixture workspace, asked for a coordinate the fixture holds and for a coordinate its guard rejects, captured before the table existed and asserted after. That golden is what proves the fifty routes still serve the same bytes in the same key order, and no shorter artifact can prove it. |
| Dashboard read routes driven from one table (production) | 202,814 | -22 | 202,792 | Issue #324: fifty dashboard read routes were the same handler written fifty times, and are now fifty rows of one declarative table plus one factory that builds them; two further routes join it — the effective-settings read (#309) and the live pipeline, now a projection of the same read that serves the live execution plan (#241). Production falls by 22 and production logic by 49: the collapse removes 640 lines of handler bodies and the table's rows put most of them back as data, because a row carries what each route serves and how it degrades — the information the deleted docstrings held. The total rises by 2,895 for the response snapshot: one recorded status and body per table route over the standard fixture workspace, asked for a coordinate the fixture holds and for a coordinate its guard rejects, captured before the table existed and asserted after. That golden is what proves the fifty routes still serve the same bytes in the same key order, and no shorter artifact can prove it. |
| Dashboard read routes driven from one table (production logic) | 120,484 | -49 | 120,435 | Issue #324: fifty dashboard read routes were the same handler written fifty times, and are now fifty rows of one declarative table plus one factory that builds them; two further routes join it — the effective-settings read (#309) and the live pipeline, now a projection of the same read that serves the live execution plan (#241). Production falls by 22 and production logic by 49: the collapse removes 640 lines of handler bodies and the table's rows put most of them back as data, because a row carries what each route serves and how it degrades — the information the deleted docstrings held. The total rises by 2,895 for the response snapshot: one recorded status and body per table route over the standard fixture workspace, asked for a coordinate the fixture holds and for a coordinate its guard rejects, captured before the table existed and asserted after. That golden is what proves the fifty routes still serve the same bytes in the same key order, and no shorter artifact can prove it. |
| Rust executable lines counted (total) | 455,909 | +124 | 456,033 | Issue #324: the Rust counter in `tools/line_budget.py` — comment stripping that follows nested block comments and blanks string bodies, and the skip over an item carrying `#[cfg(test)]` — with the fixture that pins each of those shapes. Production is unchanged: the tool and its test sit outside the runtime package. |
| The field round decomposed into phases (total) | 456,033 | +555 | 456,588 | Issue #316: `evolve_field_round` was a single 1,248-line function, and six nested functions read its locals for free. It is now a facade over four named phases in four modules. The delta is what explicit inputs and outputs cost: six typed values — the round's expanded state, the candidate field, the running tallies, the resolved tournament, the crowning verdict, and the settlement — plus the signatures of the phases and their named steps, and one import block per module. Behaviour is unchanged: all eight mock-evolve golden lanes and the reader-parity snapshots pass without recapture. |
| The field round decomposed into phases (production) | 202,792 | +548 | 203,340 | Issue #316: the same change. Almost all of it is runtime source, because the only test edits name the new home of a call site that moved. |
| The field round decomposed into phases (production logic) | 113,803 | +278 | 114,081 | Issue #316: the net across six files. `evolve/field.py` loses 910 executable lines and is left a 59-line facade. `evolve/settlement.py` adds 441, which is the settlement tail the issue asks for — the module held only result dataclasses before. `evolve/field_execution.py` adds 350, `evolve/field_candidates.py` 258, `evolve/gate.py` 112, and `evolve/generation_phase.py` 27 for the round-state type. A closure reads its enclosing locals without declaring them; a function must declare, receive, and return them, and that accounting is the whole increase. |
| Index write statements derived from the schema (total) | 456,588 | +254 | 456,842 | Issue #324: `tests/test_index_statements.py`, which holds each writer's descriptor against the DDL and pins the three re-ingest departures that a column-by-column rewrite would erase — the preserved tournament link and match tag, the ratings columns the Elo fold owns, and the field status only the settled round knows. Production falls by 4 and production logic by 81, and both machine limits ratchet down to the measured totals: `index/ingest.py` loses 143 executable lines as fourteen hand-written column lists become descriptor calls, against 62 added to `index/schema.py` for the descriptor and the column derivation. |
| Ledger arithmetic and append-only rows (total) | 456,842 | +214 | 457,056 | Issue #324: `--check-ledger` in `tools/line_budget.py` — the table parser, the per-row sum and per-measurement chaining rules, the last-row-against-the-limit rule, and the base-revision comparison that names every row the working tree dropped — with its six fixture cases, the check over the repository's own ledger, and the second step in the line-budget job. Production is unchanged: the tool and its test sit outside the runtime package. |
| Record enumeration through one typed reader (total) | 457,056 | +329 | 457,385 | Issue #324, closing issue #317: thirty-nine record walks across twenty-four modules become calls to `generation_ids` / `run_entry_ids` / `round_indices` in `zicato.workspace.reads`, which read through the new `StorageBackend.list_namespaces`. Production logic falls by 95 and production by 14: the deleted walks and their four inconsistent sorts outweigh the three readers, the namespace listing in both backends, and the `WorkspaceLayout.rounds_dir` accessor that collapses the round path's second definition. The total rises by 329 for the coverage the consolidation needs: 206 lines for the structural guard that parses every module under `src/zicato` and fails on a record walk outside the reader (a grep cannot tell a record enumeration from a telemetry-file glob, so it is an AST pass carrying its own planted-walk test), 89 for the workspace-reader golden's new labels pinning each enumeration's order against a fixture whose ids sort differently under numeric and lexical rules, and 34 for the storage conformance tests that hold the two backends' namespace listings to one answer. |
| Record enumeration through one typed reader (production) | 203,336 | -14 | 203,322 | Issue #324, closing issue #317: thirty-nine record walks across twenty-four modules become calls to `generation_ids` / `run_entry_ids` / `round_indices` in `zicato.workspace.reads`, which read through the new `StorageBackend.list_namespaces`. Production logic falls by 95 and production by 14: the deleted walks and their four inconsistent sorts outweigh the three readers, the namespace listing in both backends, and the `WorkspaceLayout.rounds_dir` accessor that collapses the round path's second definition. The total rises by 329 for the coverage the consolidation needs: 206 lines for the structural guard that parses every module under `src/zicato` and fails on a record walk outside the reader (a grep cannot tell a record enumeration from a telemetry-file glob, so it is an AST pass carrying its own planted-walk test), 89 for the workspace-reader golden's new labels pinning each enumeration's order against a fixture whose ids sort differently under numeric and lexical rules, and 34 for the storage conformance tests that hold the two backends' namespace listings to one answer. |
| Record enumeration through one typed reader (production logic) | 114,000 | -95 | 113,905 | Issue #324, closing issue #317: thirty-nine record walks across twenty-four modules become calls to `generation_ids` / `run_entry_ids` / `round_indices` in `zicato.workspace.reads`, which read through the new `StorageBackend.list_namespaces`. Production logic falls by 95 and production by 14: the deleted walks and their four inconsistent sorts outweigh the three readers, the namespace listing in both backends, and the `WorkspaceLayout.rounds_dir` accessor that collapses the round path's second definition. The total rises by 329 for the coverage the consolidation needs: 206 lines for the structural guard that parses every module under `src/zicato` and fails on a record walk outside the reader (a grep cannot tell a record enumeration from a telemetry-file glob, so it is an AST pass carrying its own planted-walk test), 89 for the workspace-reader golden's new labels pinning each enumeration's order against a fixture whose ids sort differently under numeric and lexical rules, and 34 for the storage conformance tests that hold the two backends' namespace listings to one answer. |
| Workspace configuration through one loader (total) | 457,385 | +389 | 457,774 | Issue #324, closing issue #52: thirteen sites across twelve modules opened and parsed the workspace root's `config.json` themselves, disagreeing about where it is and about what an absent or malformed one means; they now call `read_workspace_config` in `zicato.workspace.config_io`, which returns a typed `WorkspaceConfig`. Production logic falls by 3 and its limit ratchets down to 113,902: the deleted parses, existence checks, shape re-checks and duplicated auxiliary-model resolvers outweigh the loader itself. The total rises by 389 for the coverage the consolidation needs — 189 lines for the structural guard that parses every module under `src/zicato` and fails on a second reader, which is an AST pass because a grep cannot tell the workspace config from the per-epoch record of the same name, and which carries its own planted-read test; and 120 for the loader's own suite, which pins the absence rule, the one malformed error, the normalization of each block and key, and the remedy `require` names. Production rises by 80, all of it in the loader module: the typed result, that normalization, the two absence policies, and the field-by-field documentation of the parsed shape, which the thirteen deleted call sites held in pieces. |
| Workspace configuration through one loader (production) | 203,322 | +80 | 203,402 | Issue #324, closing issue #52: thirteen sites across twelve modules opened and parsed the workspace root's `config.json` themselves, disagreeing about where it is and about what an absent or malformed one means; they now call `read_workspace_config` in `zicato.workspace.config_io`, which returns a typed `WorkspaceConfig`. Production logic falls by 3 and its limit ratchets down to 113,902: the deleted parses, existence checks, shape re-checks and duplicated auxiliary-model resolvers outweigh the loader itself. The total rises by 389 for the coverage the consolidation needs — 189 lines for the structural guard that parses every module under `src/zicato` and fails on a second reader, which is an AST pass because a grep cannot tell the workspace config from the per-epoch record of the same name, and which carries its own planted-read test; and 120 for the loader's own suite, which pins the absence rule, the one malformed error, the normalization of each block and key, and the remedy `require` names. Production rises by 80, all of it in the loader module: the typed result, that normalization, the two absence policies, and the field-by-field documentation of the parsed shape, which the thirteen deleted call sites held in pieces. |
| One diff renderer for both generation stores (total) | 457,774 | +148 | 457,922 | Issue #318: `diff_generations` returned `git diff` text from the git backend and hand-rolled `difflib` output from the directory backend, and that text goes into the proposer's prompt, so the proposer's input depended on which backend a workspace stores its source with. Both backends now read the two trees and render through one `render_source_diff`. The delta is what the shared renderer costs over the two it replaces: the per-file `diff --git` header, the binary-file notice, the missing-final-newline marker, and a line split that breaks only at a newline — the directory backend had none of them, and `git diff` cannot be asked to produce output the directory backend can match — plus the git backend's whole-tree read, one `git archive` per tree with no worktree materialised. Production carries 96 of the total and production logic 30; the remaining 52 total lines are the cross-backend conformance test, which renders one pair of trees through every backend and asserts the texts are equal. |
| One diff renderer for both generation stores (production) | 203,402 | +96 | 203,498 | Issue #318: `diff_generations` returned `git diff` text from the git backend and hand-rolled `difflib` output from the directory backend, and that text goes into the proposer's prompt, so the proposer's input depended on which backend a workspace stores its source with. Both backends now read the two trees and render through one `render_source_diff`. The delta is what the shared renderer costs over the two it replaces: the per-file `diff --git` header, the binary-file notice, the missing-final-newline marker, and a line split that breaks only at a newline — the directory backend had none of them, and `git diff` cannot be asked to produce output the directory backend can match — plus the git backend's whole-tree read, one `git archive` per tree with no worktree materialised. Production carries 96 of the total and production logic 30; the remaining 52 total lines are the cross-backend conformance test, which renders one pair of trees through every backend and asserts the texts are equal. |
| One diff renderer for both generation stores (production logic) | 113,902 | +30 | 113,932 | Issue #318: `diff_generations` returned `git diff` text from the git backend and hand-rolled `difflib` output from the directory backend, and that text goes into the proposer's prompt, so the proposer's input depended on which backend a workspace stores its source with. Both backends now read the two trees and render through one `render_source_diff`. The delta is what the shared renderer costs over the two it replaces: the per-file `diff --git` header, the binary-file notice, the missing-final-newline marker, and a line split that breaks only at a newline — the directory backend had none of them, and `git diff` cannot be asked to produce output the directory backend can match — plus the git backend's whole-tree read, one `git archive` per tree with no worktree materialised. Production carries 96 of the total and production logic 30; the remaining 52 total lines are the cross-backend conformance test, which renders one pair of trees through every backend and asserts the texts are equal. |
| The line-budget summary table checked against the config (total) | 457,877 | +107 | 457,984 | Issue #324: the table above this ledger reports each measurement's enforced limit, and the prose beside it says those are the limits `.line-budget.json` holds — but nothing read the two together, so a change that moved the config could leave the table stating the old value, and twice did. `--check-ledger` now also holds each summary row against the config: its enforced limit must equal the configured one, and its last column must equal that limit minus its baseline. A missing or unreadable row is itself an error, so deleting the table is not a way to pass. The delta is 59 lines for the check and 48 for its four cases — a limit the config does not hold, a last column that misses the subtraction, a deleted row, and the raised-limit case that is now caught twice over. Production and production logic are unchanged: the tool and its test sit outside the runtime package. |
| The event log read through one reader (total) | 457,984 | +383 | 458,367 | Issue #324: nine modules resolved a line of a run's `events.jsonl` independently and disagreed — three envelope key sets omit `kind` and `payload`, so a normalized event selected the envelope's own `payload` field as its payload case; three casing rules split a run of capitals differently; only one reader converted nested field names; and the two timestamp renderings differ on any instant with non-zero nanoseconds. The delta is what stating the one reading costs in coverage: the reader's own eighteen cases, the correspondence test holding the envelope schema equal to the supervisor's Rust declaration, and one regression per listed behaviour change, each of which fails against the readers it replaces. Production falls by 297 and production logic by 275 as six envelope resolvers, six casing rules and two timestamp renderers become one of each, and the foreign-trace sniffer's own key set narrows to the three names that mark a producer; both machine limits ratchet down to the measured totals. |
| Dashboard pickers share one interaction (total) | 458,230 | +119 | 458,349 | Issue #324: the colour-swatch picker and the typeface picker each carried the same open/close state, active-row tracking, keyboard map, outside-click close and cross-instance registry, and no test built either of them. They now read one module, which with the other consolidations in this change reduces production by 51 and production logic by 89, both limits ratcheting down to the measured totals. The total rises for two pieces of coverage: `src/zicato/dashboard/static/test/dropdown.test.mjs`, 152 lines running every assertion against both pickers so the now-shared wiring cannot break one of them silently, and 18 lines in `tests/test_entry_kind_vocabulary.py` for the guard that stops any board surface declaring a kind-label map of its own again. Both are verified red-green. |
| Generation staging derived from the working tree's bytes (total) | 458,349 | +90 | 458,439 | Issue #369: `git add` skips re-hashing a file whose stat data still matches the index entry standing for it, and the git generation store lays each generation's source tree over the previous generation's files with every modification time preserved by the copy — so a change leaving a file's size alone could reach `git add` looking untouched, and the generation committed the blob already staged for that path. The bytes then never reach the object store, so the change is absent from the commit and from `diff_generations`, which is what the proposer reads. Both writers now stage through `_stage_working_tree`, which empties the index before `git add -A` so the commit is a function of the bytes on disk. Production carries 26 of the total and production logic 3; the remaining 64 total lines are the regression test, which pins one modification time across two same-size source trees and narrows git's stat comparison to the fields a copy preserves, making the collision that the intermittent failure hit by chance happen on every run. |
| Generation staging derived from the working tree's bytes (production) | 202,903 | +26 | 202,929 | Issue #369: `git add` skips re-hashing a file whose stat data still matches the index entry standing for it, and the git generation store lays each generation's source tree over the previous generation's files with every modification time preserved by the copy — so a change leaving a file's size alone could reach `git add` looking untouched, and the generation committed the blob already staged for that path. The bytes then never reach the object store, so the change is absent from the commit and from `diff_generations`, which is what the proposer reads. Both writers now stage through `_stage_working_tree`, which empties the index before `git add -A` so the commit is a function of the bytes on disk. Production carries 26 of the total and production logic 3; the remaining 64 total lines are the regression test, which pins one modification time across two same-size source trees and narrows git's stat comparison to the fields a copy preserves, making the collision that the intermittent failure hit by chance happen on every run. |
| Generation staging derived from the working tree's bytes (production logic) | 113,408 | +3 | 113,411 | Issue #369: `git add` skips re-hashing a file whose stat data still matches the index entry standing for it, and the git generation store lays each generation's source tree over the previous generation's files with every modification time preserved by the copy — so a change leaving a file's size alone could reach `git add` looking untouched, and the generation committed the blob already staged for that path. The bytes then never reach the object store, so the change is absent from the commit and from `diff_generations`, which is what the proposer reads. Both writers now stage through `_stage_working_tree`, which empties the index before `git add -A` so the commit is a function of the bytes on disk. Production carries 26 of the total and production logic 3; the remaining 64 total lines are the regression test, which pins one modification time across two same-size source trees and narrows git's stat comparison to the fields a copy preserves, making the collision that the intermittent failure hit by chance happen on every run. |
| Board progress bar and page-head margin (total) | 455,728 | +44 | 455,772 | Issues #359 and #366: two drift-class defects where the correct behaviour was already established elsewhere. The board view carried a private copy of the progress-ratio helper without the terminal-run guard, so a finished run painted the share of its wall-clock budget it had used; two views wrapped their page head in an unstyled class and lost the 18px margin the other sixteen carry. Production falls by 13 and production logic by 11 — the deleted copy outweighs one import line — and both limits ratchet down to the measured totals. The total rises for the regression coverage: a board-view test asserting a full bar and percent readout for a run terminal by status token and by board count alike, and a page-head assertion in each of the two views' own suites pinning the styled class present and the unstyled one absent. All three fail against the previous code. |
| The test suite tiered by measured runtime (total) | 455,026 | +385 | 455,411 | A default `pytest` took 7 minutes 24 seconds over 5,985 tests, and seven of them held five of those minutes: four Monte Carlo characterizations of the decision procedure at 33 to 140 seconds each, the racing convergence oracle, the gauntlet evidence-gate end-to-end run and the cascade smoke run. The `slow` marker names that measurement, taken SERIALLY because a reading under `-n auto` is two to three times the test's own cost on a busy box and would make the tier's membership depend on what else was running. A bare `pytest` now runs 5,984 tests in 1 minute 16 seconds. `make test` and `tools/parity.sh`'s PYTEST gate still mean both tiers; a pull request runs the default tier alone and `.github/workflows/slow-tier.yml` runs the other nightly against main. The rise is coverage of the tier itself — `tests/test_slow_tier_registry.py`, which collects the marked set in a child session and holds it equal to a declared list of node ids and their measured seconds, so a mark added or dropped without its row reds a test instead of silently narrowing the default run. The tier is deselected by a hook in `tests/conftest.py` rather than by a `not slow` term in `addopts`, and only when the session named no path and no `-m`: an `addopts` term applies to every invocation, so naming a `slow` test ran nothing and exited 0, and naming a file holding one silently halved it — which is what the two-oracles rule does. Four more registry tests pin the invocation forms and fail against the `addopts` design. The remainder is the marker documentation the change makes load-bearing, in `pyproject.toml`, the `Makefile`, `tools/parity.sh` and the testing chapter. Production and production logic are untouched. |
| The tests a change can reach, selected statically (total) | 455,411 | +857 | 456,268 | The full suite is the merge gate, and iterating against it costs minutes per attempt even after the runtime tiering. `tools/affected_tests.py` diffs a ref range, builds the import graph of the four tracked packages by parsing rather than importing, and prints the test files whose transitive imports reach a changed module — reading three edges no import statement shows: a `-m` pair off a spawn's argv, a literal `importlib.import_module`, and a dotted path a test names in its own text. Where it cannot establish what a change reaches it answers with the whole suite, so the failure it can produce is a slow run rather than a missed test. Across the last ten merged changes it narrows four and every change's own test files fall inside its selection; three were checked by planting the change red and running the WHOLE suite to measure what actually failed, and every failing file was inside the selection. The tool and its tests are most of it: the tests pin every escape hatch and every added edge, and caught the runner's worker spawn being missed because `create_subprocess_exec` passes its argv as positional arguments rather than as a list, and pin BOTH halves of the answer for a prose-only change — that it reaches no test, and that the tool still prints `tests/`, because `pytest $(...)` with an empty argument list would collect everything. A review reconciled those two the wrong way round once. The rest is the `make` target, the CI argument that collects the new tests, the testing chapter's section on the tool, and `tools/test_workflows.py`. That last one is a debt this change incurred and repaid: two edits to CI's Pytest step went wrong on rebase, the second leaving a step with a name and no `run`, which GitHub rejects by concluding the run with ZERO jobs. PyYAML accepts both shapes, so a parse check passes. The guard asserts what GitHub requires instead — no repeated key, a unique step name per job, one of `run` or `uses` per step — and is checked red against both real files. It is not a gate and is not wired into CI. Production and production logic are untouched. |
| A board unit run through the worker entry in this interpreter (total) | 456,268 | +696 | 456,964 | The default test tier drives about 1,080 board units, each a few milliseconds of work behind a median 0.475 s of interpreter startup and import: 560 s of aggregate subprocess wall time against 594 s of measured test time, so the process boundary rather than the work is most of what the tier costs. `worker_transport.use_worker_launcher` is one seam that runs a unit through `zicato._tournament_worker.main` in the calling interpreter, with the same args file and the same result file. Production never reaches it — `launch_worker` calls `spawn_worker_subprocess`, which owns the `python -m zicato._tournament_worker` argv and the `start_new_session=True` that gives a worker its own process group — and all twelve parity gates stay GREEN with no golden recaptured. Seven test modules opt in and the default tier falls from 1:19 to 0:46; the modules whose subject IS the boundary keep the real child, as do both oracles, and `InlineWorker` RAISES on `pid`, `terminate` and `kill` so one of those cannot pass without a worker by mistake. Of the total, 276 are production and the rest is `tests/test_worker_inline_seam.py`, which runs one unit BOTH ways and holds the two loss profiles equal field for field, and which walks all 151 loaded `zicato.*` modules to hold their 430 module-level container values unchanged across a unit. That enumeration is what closes the leak surface: it caught the judge-error register reporting eight invocations and six errors for a judge the unit never called, and the deferred-role register cleared on the way in and never put back. Each register is also checked alone, and each check fails by name when its restore is removed. |
| A board unit run through the worker entry in this interpreter (production) | 200,255 | +276 | 200,531 | The default test tier drives about 1,080 board units, each a few milliseconds of work behind a median 0.475 s of interpreter startup and import: 560 s of aggregate subprocess wall time against 594 s of measured test time, so the process boundary rather than the work is most of what the tier costs. `worker_transport.use_worker_launcher` is one seam that runs a unit through `zicato._tournament_worker.main` in the calling interpreter, with the same args file and the same result file. Production never reaches it — `launch_worker` calls `spawn_worker_subprocess`, which owns the `python -m zicato._tournament_worker` argv and the `start_new_session=True` that gives a worker its own process group — and all twelve parity gates stay GREEN with no golden recaptured. Seven test modules opt in and the default tier falls from 1:19 to 0:46; the modules whose subject IS the boundary keep the real child, as do both oracles, and `InlineWorker` RAISES on `pid`, `terminate` and `kill` so one of those cannot pass without a worker by mistake. The 276 are `src/zicato/testing/worker_inline.py` and the launcher seam in `worker_transport.py`, against which the runner's own spawn block is deleted. |
| A board unit run through the worker entry in this interpreter (production logic) | 110,738 | +102 | 110,840 | The default test tier drives about 1,080 board units, each a few milliseconds of work behind a median 0.475 s of interpreter startup and import: 560 s of aggregate subprocess wall time against 594 s of measured test time, so the process boundary rather than the work is most of what the tier costs. `worker_transport.use_worker_launcher` is one seam that runs a unit through `zicato._tournament_worker.main` in the calling interpreter, with the same args file and the same result file. Production never reaches it — `launch_worker` calls `spawn_worker_subprocess`, which owns the `python -m zicato._tournament_worker` argv and the `start_new_session=True` that gives a worker its own process group — and all twelve parity gates stay GREEN with no golden recaptured. Seven test modules opt in and the default tier falls from 1:19 to 0:46; the modules whose subject IS the boundary keep the real child, as do both oracles, and `InlineWorker` RAISES on `pid`, `terminate` and `kill` so one of those cannot pass without a worker by mistake. The 102 executable lines are the inline worker's thread launch, the snapshot and restore of the four process-wide registers a fresh worker process would start empty, its four refusals, and the launcher indirection. |
| Subprocess-accurate workers and pull-request oracle gates (total) | 456,964 | -640 | 456,324 | Issues #383 and #384: the unsafe in-process worker and its state-restoration suite are removed, so every board-unit test uses the process boundary that enforces isolation and kill semantics. The pull-request workflows add visible statistical, end-to-end, and dashboard JavaScript results with structural tests that pin their triggers and commands. The net reduction includes the deleted launcher seam, its opt-in fixtures, and its equivalence tests. |
| Subprocess-accurate workers and pull-request oracle gates (production) | 200,531 | -276 | 200,255 | Issues #383 and #384: the inline worker and launcher indirection are removed, and the runner again launches `zicato._tournament_worker` directly with `start_new_session=True`. Pull-request workflow coverage adds no production source. |
| Subprocess-accurate workers and pull-request oracle gates (production logic) | 110,840 | -102 | 110,738 | Issues #383 and #384: the production reduction is the removed in-process worker state management and launcher indirection. The runner retains the direct subprocess launch required for process isolation. |
| Durable Ladder query reservations (total) | 456,324 | +1,027 | 457,351 | Issue #380: the tournament runner evaluates the training slice before it schedules holdout work, and a strict epoch-local store atomically charges each permitted holdout query first. Reservation identities, persisted pre-charge budgets, fail-closed locking, strict decoding, and single-use settlement prevent crashes, reuse, or foreign tokens from restoring capacity or publishing uncharged evidence. Most of the increase is adversarial coverage of initialization, write failure, interruption, concurrency, process isolation, exhaustion, release visibility, evidence-pair consistency, and the corresponding deterministic parity evidence. |
| Durable Ladder query reservations (production) | 200,255 | +290 | 200,545 | Issue #380: production code gains the strict reservation store, train-first scheduling, and explicit audit fields. The pure Ladder decision rule remains separate from filesystem state, keeps the stored confirmation paired with its actual best scalar, and the runner starts no holdout work after a train rejection, exhausted budget, failed debit, or unsupported cross-process lock. |
| Durable Ladder query reservations (production logic) | 110,738 | +305 | 111,043 | Issue #380: executable growth is the atomic state protocol, identity and corruption checks, process serialization, evidence-pair consistency, and the scheduling boundary that makes the debit durable before evidence access. Replaced docstrings and comments make the executable-line delta slightly larger than the total production-line delta. |
| Replayable field settlement and lineage-authoritative index (total) | 457,351 | +3,454 | 460,805 | Issues #379 and #54: a retained receipt makes every resolved tournament recoverable across outcome, lineage, champion-marker, journal, bracket, promotion-hook, and grouped index-projection boundaries. The increase includes crash injection at every write, receipt-tampering refusals, directory and Git source cleanup, contract-roll recovery, health and dashboard visibility, index reconstruction, and documentation. |
| Replayable field settlement and lineage-authoritative index (production) | 200,545 | +1,762 | 202,307 | Issues #379 and #54: production gains one field-specific receipt protocol, atomic lineage resolution and discard, source cleanup through `GenerationStore`, a grouped derived-index transaction, and operator-visible repair states. Settlement remains field-specific rather than introducing a generic transaction layer; lineage is the sole authority for parentage and promotion in the index. |
| Replayable field settlement and lineage-authoritative index (production logic) | 111,043 | +1,616 | 112,659 | Issues #379 and #54: executable growth is strict receipt validation, ordered idempotent replay, at-most-once promotion-hook delivery, conservative receiptless-field cleanup, verification of Git ref and worktree pruning, and atomic index projection. The receipt deliberately carries one independent primary-champion checksum because multi-promotion outcomes do not identify the single champion-marker target. |
| Optional contract-bound Goldfive integration (total) | 460,805 | +2,057 | 462,862 | Issue #386: Goldfive owns and validates its runtime configuration document, while Zicato activates the integration only for adapters that declare it. Evaluator, adapter, and integration identities prevent cached measurements from crossing implementation changes. Regression coverage exercises generic adapters, the public judge-only path, frozen-contract validation, malformed configuration, process transport, Builder and API surfaces, environment-backed credentials, explicit epoch selection, and pre-spend checks. |
| Optional contract-bound Goldfive integration (production) | 202,307 | +492 | 202,799 | Issue #386: the lazy integration bridge, capability-gated worker projection, generic adapter identity, frozen-contract verification, explicit runtime settings, dependency validation, and Builder surface replace Zicato's private Goldfive schema and ADK judge-only implementation. Adapters that do not declare Goldfive carry no Goldfive configuration or import requirement. Tournament commands validate and load the selected frozen epoch before constructing a runtime, and absolute worker-permit paths preserve the configured host-wide ceiling across working directories. |
| Optional contract-bound Goldfive integration (production logic) | 112,659 | +602 | 113,261 | Issue #386: executable growth covers capability and dependency checks, credential-name transport, upstream runtime projection, full adapter identity, verifiable implementation revisions, frozen-contract comparison, selected-epoch tournament loading, and measured worker-preparation failures. Deleting the private configuration parser and judge-only path offsets part of that growth. |
| Transcript reconstruction owned by the query layer (total) | 462,862 | -67 | 462,795 | Issue #402: the reconstructor moves into the query package, and the injection seam that carried it across the layer boundary — the callable parameter on both conversation readers, the guarded import and availability flag in the endpoints, the two unavailable responses, and their tests — is deleted. |
| Transcript reconstruction owned by the query layer (production) | 202,799 | -58 | 202,741 | Issue #402: production loses the guarded import, the availability flag, the two unavailable-response branches, the two reader degrade branches, and the four parameter threadings; the moved module carries the same lines at its new path. |
| Transcript reconstruction owned by the query layer (production logic) | 113,261 | -41 | 113,220 | Issue #402: the executable reduction is the deleted try/except import block, the availability branches in the endpoints, and the `reconstruct is None` degrades in both readers. |
| Foe as the only proposal runtime (total) | 462,795 | -1,178 | 461,617 | Issues #297/#298/#299/#300/#303: Foe becomes the only proposal runtime, and every superseded implementation goes with the change — the ADK proposer agent and both its paths, the single-shot prompt-and-parse engine, the two prompt templates and the two renderers that filled them, the executable proposer-directory module and the spec field that hashed it, and the native-slate capability. What replaces them was already in the tree: one episode per candidate, one request builder for the loop and the CLI. The reduction is what four implementations of one protocol cost. Coverage grows against it: the episode's own suite, the standalone command's, the pre-spend gate's, and the stand-in proposal runtime the loop suites now declare, which runs a real episode per candidate where a fixture pinned a text shim. Production grows by the two surfaces the single runtime needs an operator to reach: `zicato init` scaffolds the block, and the gate names the field to fill rather than letting a round discover it. All three limits ratchet down to the measured totals. |
| Foe as the only proposal runtime (production) | 202,741 | -701 | 202,040 | Issues #297/#298/#299/#300/#303: Foe becomes the only proposal runtime, and every superseded implementation goes with the change — the ADK proposer agent and both its paths, the single-shot prompt-and-parse engine, the two prompt templates and the two renderers that filled them, the executable proposer-directory module and the spec field that hashed it, and the native-slate capability. What replaces them was already in the tree: one episode per candidate, one request builder for the loop and the CLI. The reduction is what four implementations of one protocol cost. Coverage grows against it: the episode's own suite, the standalone command's, the pre-spend gate's, and the stand-in proposal runtime the loop suites now declare, which runs a real episode per candidate where a fixture pinned a text shim. Production grows by the two surfaces the single runtime needs an operator to reach: `zicato init` scaffolds the block, and the gate names the field to fill rather than letting a round discover it. All three limits ratchet down to the measured totals. |
| Foe as the only proposal runtime (production logic) | 113,220 | -310 | 112,910 | Issues #297/#298/#299/#300/#303: Foe becomes the only proposal runtime, and every superseded implementation goes with the change — the ADK proposer agent and both its paths, the single-shot prompt-and-parse engine, the two prompt templates and the two renderers that filled them, the executable proposer-directory module and the spec field that hashed it, and the native-slate capability. What replaces them was already in the tree: one episode per candidate, one request builder for the loop and the CLI. The reduction is what four implementations of one protocol cost. Coverage grows against it: the episode's own suite, the standalone command's, the pre-spend gate's, and the stand-in proposal runtime the loop suites now declare, which runs a real episode per candidate where a fixture pinned a text shim. Production grows by the two surfaces the single runtime needs an operator to reach: `zicato init` scaffolds the block, and the gate names the field to fill rather than letting a round discover it. All three limits ratchet down to the measured totals. |
| Proposer tool bodies with no caller (total) | 461,617 | -2,255 | 459,362 | Issue #415: the proposal-runtime replacement left the proposer's tool registry standing, and after it nothing called most of the bodies in it. Seven read tools, the three-tool redacted train-slice query surface and its module, the registry tuple, the mutable-root resolution two of the deleted readers needed, and the copy-me agent that imported three of them are removed with their tests and their documentation. What an episode is served is unchanged: `mutation_usage` and `validate_patches`, the two host tools named in `SANCTIONED_TOOLS`, with `grep_mutable` as the search behind the first. All three limits ratchet down to the measured totals. |
| Proposer tool bodies with no caller (production) | 202,040 | -1,172 | 200,868 | Issue #415: the proposal-runtime replacement left the proposer's tool registry standing, and after it nothing called most of the bodies in it. Seven read tools, the three-tool redacted train-slice query surface and its module, the registry tuple, the mutable-root resolution two of the deleted readers needed, and the copy-me agent that imported three of them are removed with their tests and their documentation. What an episode is served is unchanged: `mutation_usage` and `validate_patches`, the two host tools named in `SANCTIONED_TOOLS`, with `grep_mutable` as the search behind the first. All three limits ratchet down to the measured totals. |
| Proposer tool bodies with no caller (production logic) | 112,910 | -573 | 112,337 | Issue #415: the proposal-runtime replacement left the proposer's tool registry standing, and after it nothing called most of the bodies in it. Seven read tools, the three-tool redacted train-slice query surface and its module, the registry tuple, the mutable-root resolution two of the deleted readers needed, and the copy-me agent that imported three of them are removed with their tests and their documentation. What an episode is served is unchanged: `mutation_usage` and `validate_patches`, the two host tools named in `SANCTIONED_TOOLS`, with `grep_mutable` as the search behind the first. All three limits ratchet down to the measured totals. |
| A reader for the Foe episode log (total) | 459,362 | +1,462 | 460,824 | Issue #304: a proposal episode writes a Foe `episode.jsonl`, a format the Goldfive/ADK reader cannot read at all — its envelope carries `type` and `data` where an ADK line carries a payload case, so the old reader returned an empty transcript for every proposer episode. `query/foe_episode.py` reads that format and states the derived-message rule the log specifies; `reconstruct_transcript` selects a reader from the file's first line and projects an episode onto the same `Transcript` shape the ADK path produces, so every existing caller and the whole rendering path are unchanged. A generation named without a board entry now resolves to its episode log, which is where a proposer transcript comes from and the only place it may come from. Most of the rise is coverage: three fixtures copied from Foe's own viewer tests hold the two readers to one reading of the same bytes, by recomputing each request's message list and comparing it with the list that request recorded; three ADK streams and the payloads the reader produced for them before this change pin the ADK path field for field, `unresolved_ids` included. |
| A reader for the Foe episode log (production) | 200,868 | +757 | 201,625 | Issue #304: the addition is the episode-log reader (`query/foe_episode.py`), the projection of an episode onto the shared `Transcript` shape and the reader selection in `query/transcript_reconstruction.py`, the proposal-episode resolver in `query/events_index.py`, the two layout accessors that give the episode directory one path definition, and the query parameters the conversation endpoint passes through. The ADK reader is unchanged. |
| A reader for the Foe episode log (production logic) | 112,337 | +450 | 112,787 | Issue #304: the executable growth is the episode-log parse, the derived-message rule with its summarization exclusion, its inner-call exclusion and its post-compaction continuation rendering, the walk that turns those messages into turns and hangs each tool call off the episode that issued it, and the directory search that resolves one generation's episode and refuses a file that is not one. |
| A static page for the proposal episode (total) | 460,824 | +955 | 461,779 | Issue #305: Foe renders a finished episode to one self-contained page, and a settled proposal episode now has that page beside its log, linked from the candidate's proposal header. Most of the rise is coverage: the page is written when an episode settles and is rendered from that episode's own log, a forced export failure leaves the round's experiment and its log identical to a run with no export, the round gains exactly one file and no record, a coordinate naming a foreign path is refused, and the panel captions the log and the by-hand command when there is no page. The Foe stand-in answers the export invocation so no test needs a Rust build. |
| A static page for the proposal episode (production) | 201,625 | +420 | 202,045 | Issue #305: Foe renders a finished episode to one self-contained page, and a settled proposal episode now has that page beside its log, linked from the candidate's proposal header. The addition is the export seam that runs the binary and writes the page, the two readers behind the availability payload and the served page, the layout accessor for the page's path, the two routes and their payload contract, and the link row with its digest fold in the candidate view. |
| A static page for the proposal episode (production logic) | 112,787 | +220 | 113,007 | Issue #305: Foe renders a finished episode to one self-contained page, and a settled proposal episode now has that page beside its log, linked from the candidate's proposal header. The executable growth is the bounded subprocess with its five failure returns, the availability read and its configured-binary lookup, the two route handlers with their coordinate guards, and the row that branches between the link and the caption. |
| One owner for every workspace location (total) | 461,777 | +492 | 462,269 | Issue #407: `WorkspaceLayout` becomes the only declaration of where each stored artifact lives, and the twenty-four `*_key` helpers resolve it against an empty root instead of re-spelling the same directory and file names. Most of the rise is the correspondence registry that keeps the two from drifting again: it names each key beside the layout member that owns its location and holds all twenty-four against that member, against the absolute path a real backend write lands at, and against the spelling the key already has in operators' workspaces, with a fourth guard that fails when a new key helper is not entered. |
| One owner for every workspace location (production) | 202,016 | +212 | 202,228 | Issue #407: the layout gains the seventeen `runtime/` declarations moved out of `zicato.runtime.paths` and the round-directory declaration moved out of `zicato.epoch.round_log`, each carrying the prose that states what the file is for and when it is absent. `zicato.runtime.paths` and `zicato.query.paths.WorkspacePaths` keep their public names and resolve through the layout, which is what leaves one declaration per location while no caller changes. |
| One owner for every workspace location (production logic) | 113,000 | +74 | 113,074 | Issue #407: the executable growth is one accessor per runtime location on the layout, set against the joins removed from the two path modules and the twenty-four key helpers, plus the relative-rooted layout and the key renderer the helpers read locations through, and the id guard that keeps an enumeration from addressing outside the record subtree it names. |
| One owner for the below-noise-floor assessment (total) | 462,269 | +351 | 462,620 | Issue #388: the below-noise-floor assessment — whether `promote_margin` sits inside the measured A/A noise, what to raise it to, which floor statistic backs that value, and whether acting on it would raise the gate — was derived at four sites, two of which carried their own copy of the 2.5 sigma multiple. `tournament/calibration.py` gains 135 lines for the `MarginNoiseAssessment` type, its two constructors, and the prose stating which statistic decides the comparison and which scales the recommendation. The four rendering sites lose 21 between them: `reflection/practices.py` 15, `reflection/findings.py` 14, and `evolve/round_prepare.py` 3, against 11 added in `health/diagnostics.py` where the finding conversion now names what it converts. Production logic rises by 34, the dataclass and its two constructors set against the arithmetic deleted at the four sites. The total rises by a further 237 for the correspondence suite `tests/test_noise_floor_one_owner.py`, which parses every module under `src/zicato` to assert the multiple has one definition and substitutes a value into the domain assessment to assert each of the four surfaces reports the substituted value rather than one it derived itself. |
| One owner for the below-noise-floor assessment (production) | 202,228 | +114 | 202,342 | Issue #388: the below-noise-floor assessment — whether `promote_margin` sits inside the measured A/A noise, what to raise it to, which floor statistic backs that value, and whether acting on it would raise the gate — was derived at four sites, two of which carried their own copy of the 2.5 sigma multiple. `tournament/calibration.py` gains 135 lines for the `MarginNoiseAssessment` type, its two constructors, and the prose stating which statistic decides the comparison and which scales the recommendation. The four rendering sites lose 21 between them: `reflection/practices.py` 15, `reflection/findings.py` 14, and `evolve/round_prepare.py` 3, against 11 added in `health/diagnostics.py` where the finding conversion now names what it converts. Production logic rises by 34, the dataclass and its two constructors set against the arithmetic deleted at the four sites. The total rises by a further 237 for the correspondence suite `tests/test_noise_floor_one_owner.py`, which parses every module under `src/zicato` to assert the multiple has one definition and substitutes a value into the domain assessment to assert each of the four surfaces reports the substituted value rather than one it derived itself. |
| One owner for the below-noise-floor assessment (production logic) | 113,074 | +34 | 113,108 | Issue #388: the below-noise-floor assessment — whether `promote_margin` sits inside the measured A/A noise, what to raise it to, which floor statistic backs that value, and whether acting on it would raise the gate — was derived at four sites, two of which carried their own copy of the 2.5 sigma multiple. `tournament/calibration.py` gains 135 lines for the `MarginNoiseAssessment` type, its two constructors, and the prose stating which statistic decides the comparison and which scales the recommendation. The four rendering sites lose 21 between them: `reflection/practices.py` 15, `reflection/findings.py` 14, and `evolve/round_prepare.py` 3, against 11 added in `health/diagnostics.py` where the finding conversion now names what it converts. Production logic rises by 34, the dataclass and its two constructors set against the arithmetic deleted at the four sites. The total rises by a further 237 for the correspondence suite `tests/test_noise_floor_one_owner.py`, which parses every module under `src/zicato` to assert the multiple has one definition and substitutes a value into the domain assessment to assert each of the four surfaces reports the substituted value rather than one it derived itself. |
| One decoder for the per-judge row (total) | 462,527 | +188 | 462,715 | Issue #406: `tests/test_dashboard_phase1.py` gains 145 lines pinning what each of the five per-judge readers serves as the exact JSON text an endpoint writes, for a populated workspace and for the two ways an unindexed one degrades, so the shared decoding cannot move a field, a field's position, or a coercion unseen. The four reader tests it replaces asserted subsets of the same responses. Production carries the remaining 43. |
| One decoder for the per-judge row (production) | 202,284 | +43 | 202,327 | Issue #406: `query/_sqlite.py` gains 17 lines for `INDEX_NOT_BUILT_NOTE` and `with_index_not_built_note`, which replace the degrade note spelled out at eleven sites across six modules of the query package; naming it costs an import line in each of the four sibling modules that serve it. `query/judge_view.py` gains 18: `_judge_row` and its statement of why the three sources spell the same four fields differently, set against the three row literals it replaces and the per-side merge loop that `build_per_judge_comparison` drops by reading both sides through `build_per_judge_for_generation`. Production logic falls by one and the limit ratchets to it. |
| One canonical aggregation shared by the report and the query layer (total) | 462,715 | +565 | 463,280 | Issue #404: four quantities computed from the canonical files — a generation's per-judge weighted-loss totals, an epoch's board entries, the cumulative scalar along a lineage, and the folded round records — each had two definitions, one in the analysis-report gatherer and one in a query reader that opens the same files. `tests/test_canonical_aggregation_parity.py` carries 288 of the rise: a fixture workspace built with the canonical writers, on which the report's per-judge totals are measured against the numbers the per-entry judge table serves, its board entries against the board reader's, its round records against the execution plan's, and its cumulative scalars against the journal's trajectory table, with a further test pinning that the index-backed per-judge view still answers from `index.db` alone. Production carries the remaining 277. |
| One canonical aggregation shared by the report and the query layer (production) | 202,327 | +277 | 202,604 | Issue #404: `workspace/aggregates.py` is new, 256 lines holding one definition per quantity beside the prose naming which readers consume it and why the index-backed views do not. Exporting its nine names costs 28 lines in the package's `__init__`. Against that, `analyzer/report_data.py` falls by 28 as four local reducers become calls, `epoch/analysis.py` rises by 14 where its scalar trajectory supplies steps to the shared cumulation, and the three query readers rise by 7 between them for their imports and the sentences naming the reader they now read through. |
| One canonical aggregation shared by the report and the query layer (production logic) | 113,068 | +106 | 113,174 | Issue #404: the executable growth is 103 lines in `workspace/aggregates.py` — four readers, the judge-row decoder, and three record types — plus 20 for the export list, set against 24 lines of loops deleted from the analysis-report gatherer and the three query readers that now call them. |
| Board expectation coverage reported before spend (total) | 463,280 | +351 | 463,631 | Issue #403: the fraction of board entries carrying no expectation was judged only after a round had produced data, so a board that grades almost nothing reached the operator as a finding they had already paid for. `tests/test_check_gate.py` carries 160 of the rise for the advisory's own coverage — the entries its summary names, the count it stops naming at, the mostly-graded board it stays silent on, the workspace threshold that governs it and the health detector alike, the unparseable threshold block it survives on the defaults, the dry-run render, and the public loop boundary where it is reported with no model called. `tests/test_health_diagnostics.py` carries 28 pinning the health finding's exact wording and payload across the move. Production carries the remaining 163. |
| Board expectation coverage reported before spend (production) | 202,604 | +163 | 202,767 | Issue #403: `board/expectation_coverage.py` is new, 94 lines holding the one rule both surfaces read — which entries count as ungraded, what fraction of the board they are, the threshold that fraction must exceed, and the payload a finding carries. `check/validators.py` gains 50 for the advisory and the summary that names the ungraded entries, `check/context.py` 17 for the workspace `health` block the validator takes its threshold from, and three docstrings 4 between them where the advisory tier is characterised. `health/diagnostics.py` falls by 2, its detector becoming a rendering of the shared measurement. |
| Board expectation coverage reported before spend (production logic) | 113,174 | +54 | 113,228 | Issue #403: 36 executable lines in `board/expectation_coverage.py` for the coverage type and the measurement, 20 in `check/validators.py` for the validator and its summary helper, and 7 in `check/context.py` for the threshold read, set against the 9 the health detector drops by reading the shared measurement rather than computing its own. |
| Parity oracle reachable from the Makefile (total) | 463,631 | +13 | 463,644 | Issue #324: the parity oracle was reachable only by typing its script path, so the one merge gate `make check` does not cover was the one a contributor had to remember unaided. `make parity` runs `tools/parity.sh` with PYTEST and MYPY skipped, because `make check` runs the suite and mypy as gates of its own and running them twice doubles the oracle's slowest part for no extra signal. The delta is the target and its recipe, the `PARITY_ARGS` variable that restores the full thirteen-gate run, the comment stating which gates are skipped and why, and two lines of help text. Production and production logic are unchanged: the Makefile sits outside the runtime package. |
| One definition for the matrix grid and the percent label (total) | 463,644 | +287 | 463,931 | Issue #62: three surfaces drew the same table grid — the mutation surface, the field-diversity figure and the evals matrix — each spelling out the corner cell, the column and row headers, the on/off body cell, the mark and the blank dot, and two of them hand-rolling the same sixteen-unit mark grid; separately, a rate reached the screen through two private formatters and twelve open-coded roundings, and a duration through two private formatters beside the canonical one. `static/js/matrix.js` is new, 106 lines holding one builder per grid element beside the prose naming the interaction detail each call site keeps: the mutation surface pins a row and a cell, the diversity figure rails the pair with the highest overlap, and the evals matrix shades a cell by its evidence and appends ghost rows. `svg.js` gains `fmtPercent` beside the numeric formatters it already owns. Production carries 91 of the rise and coverage the remaining 196: `static/test/matrix_grid.test.mjs` and its recorded fixture render five matrices — the mutation grid unpinned, with a pinned row, and with a pinned cell; the diversity grid with its railed pair; and the evals grid with its round groups and a ghost row — and compare a serialization of each against the DOM those builders produced before they shared anything, while the two panels whose duration spelling changes gain a pin apiece. |
| One definition for the matrix grid and the percent label (production) | 202,767 | +91 | 202,858 | Issue #62: `static/js/matrix.js` carries 106 of the rise and `ui.js` 3, for re-exporting the percent formatter and stating why the definition sits in `svg.js` — the figures format rates too, and `svg.js` cannot import `ui.js`, which reads from it. Against that, five modules fall by 18 between them as their private formatters and their open-coded grid markup go: `views/evals_health.js` 6, `views/board.js` 6, `svg.js` 3, `views/evals.js` 2 and `views/instrument.js` 1. |
| One definition for the matrix grid and the percent label (production logic) | 113,228 | +30 | 113,258 | Issue #62: the executable growth is the 48 lines of `static/js/matrix.js` — eleven builders and the two helpers that join a shared class with the classes and attributes a call site passes — set against the 18 the call sites drop: 6 in `svg.js`, 5 in `views/evals_health.js`, 4 in `views/board.js`, 2 in `views/evals.js` and 1 in `views/instrument.js`. |
| Composable builders for read-side test fixtures (total) | 463,931 | +302 | 464,233 | Issue #405: a read-side test built its workspace tree and its `index.db` by hand, re-spelling two things production declares once — where each file lives, which `WorkspaceLayout` declares, and what columns the index carries, which `zicato.index.schema` declares — so a fixture could drift from what the writers produce without any test noticing. `tests/_workspace_support.py` is new, 298 lines, 138 of which execute and 101 of which are docstrings stating what each builder writes and when a test should step outside them: three file writers; tree builders for the workspace, epoch, generation and run directories that take every path off the layout; the `experiment.json` record; and an index seeder that applies the real schema, rejects a column name the schema does not declare, and reproduces an older index by dropping a named column rather than by carrying a second copy of the DDL. The five files migrated onto the builders rise by 4 between them: `test_query_experiments_ledger.py` falls 10, `test_query_reader_hygiene.py` 7 and `_reader_parity_harness.py` 6, while `test_query_rating_readers.py` rises 21 and `test_query_lineage_build_once.py` 6, because naming each seeded column costs more lines than the positional tuple it replaces and is what makes a fixture fail when the schema moves. Production and production logic are unchanged, because the builders live under `tests/`. |
| Adapter declaration in the evaluation contract (total) | 463,616 | +336 | 463,952 | Issue #364: a custom harness adapter is built by operator code, and the document that code reports for subprocess reconstruction need not repeat the factory path or the constructor arguments the operator declared, so a workspace could change either — a materially different harness — while its evaluation contract stood and its generations went on being compared as though nothing had moved. `epoch/contract.py` carries 96 of the rise: a contract input for the declared adapter block, one normalizer now shared by the worker document and the declaration, the residue rule that hashes only what the declaration states and the worker document does not, and the refusal to hash a declared factory whose defining module has no readable source. Coverage carries the remaining 240. Ten register the new component in the carryover fixture, whose adapter reports a worker document naming only its factory, so a dropped declaration is observable there. The other 230 are in `tests/test_epoch_contract.py`: an adapter that reports a constant document and the changed argument that must roll its contract, an adapter that reports its declaration faithfully and the component that must not move for it, an edited implementation behind an unchanged factory path, the unreadable implementation that must stop the hash, and two recorded digests pinning the component for a workspace that declares no adapter and for one that declares the stock ADK harness. |
| Adapter declaration in the evaluation contract (production) | 202,416 | +96 | 202,512 | Issue #364: `epoch/contract.py` gains the `adapter_declaration` contract input, `_canon_adapter_document` for the normalization the worker document and the declaration now share, `_canon_adapter_declaration` for the residue that a faithful worker document reduces to nothing, and `_require_declared_factory_source` for the loud refusal. Roughly two thirds of the rise is prose stating which of the two documents is authoritative for each field and why a mirrored declaration is dropped rather than re-hashed. |
| Adapter declaration in the evaluation contract (production logic) | 112,899 | +32 | 112,931 | Issue #364: the executable growth is the declared-block comparison and the factory-source guard — the residue comprehension over the declared fields, the shared normalizer that drops `mutable_trees` and sorts integration names for both documents, and the scan of the recorded implementation sources that raises when the declared factory resolved to no readable source — set against the five inline normalization lines the shared normalizer replaces in `_canon_adapter`. |
| Swiss standings count the pairings a contestant lost (total) | 463,952 | +20 | 463,972 | Issue #337: the Swiss standing wrote a zero into every contestant's `losses` field while `wins` carried the Copeland score, so a settled five-contestant field reported four lost pairings as none. `tests/test_selection_strategies.py` carries 15 of the rise for a four-contestant field over two rounds with a known result table, asserting the wins and the losses of all four against it. Production carries the remaining 5. |
| Swiss standings count the pairings a contestant lost (production) | 202,512 | +5 | 202,517 | Issue #337: `selection/strategies/swiss.py` names the loser of each settled pairing beside the winner and tallies it into the loss map the champion-gate base already declares, which is the same map the other bracket structures fold into. Its standings docstring gains the rule a reader otherwise has to derive: a bye raises `wins` with no pairing to lose, so `wins + losses` is the pairings a contestant played plus the byes it received. |
| Swiss standings count the pairings a contestant lost (production logic) | 112,931 | +2 | 112,933 | Issue #337: the executable growth is the loser lookup and the increment that tallies it; the standings row reads the tally in place of the literal zero it held. |
| One entry order for the eval-health payload (total) | 463,972 | +10 | 463,982 | Issue #353: the per-entry instrument map was folded through a set, so an epoch whose board does not load served its `insufficient` and `runtime_cost` entries in an order that changed with the process's hash seed. `tests/test_query_eval_view.py` gains 19 lines building that payload from a seeded epoch with its board removed and pinning both lists in id order, which is the assertion that fails under the seeds the old fold reordered; two reads inside one process agreed even before the fix, so the round trip alone would not have caught it. Against that, `tests/_endpoint_snapshot_harness.py` loses 14: the endpoint snapshot compared one list sorted rather than as served, and with the order decided at the source it compares every list as served. Production carries the remaining 5. |
| One entry order for the eval-health payload (production) | 202,517 | +5 | 202,522 | Issue #353: `query/eval_view.py` sorts the union of the two sources it folds the instrument map from, and its docstring states the rule the sort holds — the payload walks this map in its own order for every entry the board does not name, so the fold decides what identical reads serve. Production logic does not move: the sort replaces the bare set expression on the same line. |
| Two declared cuts inside the library (total) | 463,982 | +135 | 464,117 | Issue #410: two dependency directions inside the library held in fact and were declared nowhere, so crossing one broke nothing. Two forbidden contracts in `pyproject.toml` state them: the twenty-four packages that model, execute, score and store import none of the eleven that decide a round, render reports, diagnose a workspace, serve reads, adjudicate the evaluation contract or drive a session; and the seven shared primitives — the storage backend, the process configuration, the utilities, the import-path resolver, the auxiliary-timeout helper, the log stream and the integration bridges — import nothing else in the library. The whole delta is configuration: 91 lines are the two stanzas' explicit source and forbidden module lists, which a forbidden contract cannot shorten to a wildcard without the source packages forbidding each other, and 42 are the prose naming the seven library packages held out of the first source list because they import something above the cut, the entry whose clearance rests on a single import, and the design constraint the second contract is the only record of. Production and production logic are unchanged: `pyproject.toml` sits outside the runtime package, and the documentation this change brings back into agreement with the contract set is excluded from every count. |
| A proposal episode edits the tree it is told to edit (total) | 464,117 | +144 | 464,261 | Issue #440: a proposal episode could not change anything, so no epoch could produce a challenger — the working copy was granted write without read, which the read-modify-write `edit` cannot use, and the mutation manifest handed the episode paths inside the read-only snapshot. `proposer/foe_request.py` carries 50 of the rise and `tests/test_proposer_contract_identity.py` the other 94. The coverage is three cases over two fixtures: the grant shape, where the working copy leads the read roots and the snapshot stays read-only; the rebased manifest, where each point's file and source root move to the writable tree while its id, line range and content stand; and a point outside the snapshot, which is left exactly as it is. `test_changing_a_grant_path_leaves_the_hash_alone` moves with the new shape rather than growing, because only the shape is fingerprinted. |
| A proposal episode edits the tree it is told to edit (production) | 202,522 | +50 | 202,572 | Issue #440: `proposer/foe_request.py` gains `rebase_mutations`, which re-addresses the mutation manifest onto the tree the episode may write, and `render_task` calls it before rendering. Roughly two thirds of the rise is prose. The function's docstring states why the manifest is the only place an episode is handed concrete file paths and why re-addressing what it is shown does not move what is checked — validation matches patches by mutation-point id and resolves pre-images against the generation root. The grants block states why the working copy is granted read as well as write, since a `grants.write` directory confers no read, and why it must lead the read roots, since a relative path argument resolves against the first one. |
| A proposal episode edits the tree it is told to edit (production logic) | 112,933 | +14 | 112,947 | Issue #440: the executable growth is `rebase_mutations` alone — its signature, the inner `_rebase` that relativizes a path against the read root and returns it untouched when it does not sit under one, the comprehension that rewrites each point's file and source root, and the one call `render_task` makes before it renders. The two edited lines cost nothing: the `dataclasses` import gains `replace` in place, and the grants line is rewritten in place. |
| An optional subsystem's failure cannot end the run (total) | 464,261 | +75 | 464,336 | Issue #441: `evolve` is meant to run without a dashboard, without the supervisor watchdog and without the hypercorn idle-coroutine workaround, and each of the three sites carried a guard narrower than the failure it was written for, so any one of them could end the invocation instead of degrading. Production carries 18 of the rise and coverage the other 57. `tests/test_evolve_supervisor_spawn.py` gains 44 for two cases — a spawn argv that cannot be built, which degrades to a warning and no process, and a module one parent deep, which resolves to no binary rather than raising — and `tests/test_harmonograf_supervisor.py` gains 13 for a missing hypercorn leaving the patch unapplied. Each of the three fails against the code as it stood, so all three pin the defect rather than restating the fix. |
| An optional subsystem's failure cannot end the run (production) | 202,572 | +18 | 202,590 | Issue #441: `cli/commands/evolve.py` carries 10 and `telemetry/harmonograf_supervisor.py` 8. In `evolve.py` the dashboard argv moves inside the guard that covers the spawn it feeds, both spawn guards widen to the exception a `None` argv[0] actually raises, and the dev-checkout parent walk is bounded. Because a bounded walk can now find no candidate at all, one name carries both facts: `dev_usable` is the candidate when there is one and it is executable, and `None` otherwise — a separate boolean beside an optional path can drift from it, and every call site would have to restate the check. In `harmonograf_supervisor.py` the hypercorn import gains its own guard. Roughly half of each file's rise is prose stating why the widened guard is the right one. |
| An optional subsystem's failure cannot end the run (production logic) | 112,947 | +6 | 112,953 | Issue #441: the executable growth is three lines guarding the hypercorn import so a missing distribution returns instead of raising, one for splitting the parent walk from the candidate it builds, and two for the membership test the optional candidate needs at the site that compares modification times. The rewritten guard clauses, the moved argv line and the renamed candidate cost nothing: each replaces a line in place. |
| Two conformance pins assert against their own subject (total) | 464,336 | +10 | 464,346 | Issue #442: two pins asserted against something other than what the implementation they pin does, so each failed on the machines the implementation was written for and neither failure was evidence about the subject. `tests/test_spawn_permit.py` carries 7 of the rise and `tests/test_proposer_validate.py` 3, and both are comment: the permit pin now measures AUTO against `_usable_cpus`, the affinity-aware count the implementation derives it from rather than the host count `os.cpu_count` reports, and states the container case that separates the two; the reader pin switches `grep -r` for `-R` and states that the former does not follow symlinks, so over a tree of symlinked sources it matched nothing and failed with an empty reader set. Production does not move: both changes are inside tests. The permit pin is weaker for it — measured against the same helper, it can no longer catch a wrong derivation — but it still pins that AUTO is what `effective_permit_count(None)` returns, that it clears the floor, and that it scales with the affinity mask. |
| Source generations reconciled against their canonical records (total) | 464,346 | +202 | 464,548 | Issue #398: `derive_generation` commits and tags a candidate's source tree before the round writes its pending lineage node and its `experiment.json`, so a process that stopped inside that interval left a Git tag and a worktree outside `epochs/` that no startup enumeration reached, still holding the identifier the next proposal allocated. `tests/test_runtime_resume.py` carries 124 of the rise: five cases over a repository seeded in exactly that state — the tag, the worktree and the branch head are gone after startup; the freed identifier is re-derived from the baseline commit rather than from the residue; a second startup finds nothing left to reconcile; a generation that does hold a record keeps its source; and the directory backend is shown to reach no such state — together with the `git rev-parse` helper they read commits through and the fixture docstring naming the interval it leaves behind. `src/zicato/runtime/resume.py` carries 77 and `src/zicato/evolve/propose_apply.py` one, a correction to the comment at the lineage write. `docs/design/STORAGE.md` gains the paragraphs describing the reconciliation and is Markdown, which reaches no count. |
| Source generations reconciled against their canonical records (production) | 202,590 | +78 | 202,668 | Issue #398: `runtime/resume.py` gains `_discard_unrecorded_source`, which compares `GenerationStore.list_generations` against the epoch's generation record directories and its lineage nodes and prunes every non-baseline source generation that neither register names; `_lineage_generation_rows`, the one lineage walk that both `_pending_lineage_groups` and that comparison read an epoch's generation nodes through; the `_BASELINE_GENERATION_ID` constant and the comment stating why the seed generation is exempt from the comparison; and the module-header and `prepare_resume` docstring text placing the reconciliation after settlement-receipt replay and before field classification. `evolve/propose_apply.py` carries one line, in the comment at the pending-lineage write, which now names where source created before that marker is reconciled instead of calling it unrecoverable. |
| Source generations reconciled against their canonical records (production logic) | 112,953 | +24 | 112,977 | Issue #398: the executable growth is 23 lines in `_discard_unrecorded_source` — the union of the two registers, the non-baseline filter over `list_generations`, the prune through `_prune_generation_records`, and the warning naming what was discarded — 13 in `_lineage_generation_rows`, one for the baseline constant, and one for the call `prepare_resume` makes. Extracting the lineage walk returns 14, because `_pending_lineage_groups` loses its own epoch lookup and its per-row type guard; the shared walk therefore costs one line less than the two inline copies it would otherwise need. `evolve/propose_apply.py` changes only a comment and adds no executable line. |
| One decoder for the generation record (total) | 464,548 | +384 | 464,932 | Issue #411: the per-generation experiment record had three decoders that disagreed — the typed reader in `epoch/journal.py`, the lossy hydrator in `epoch/analysis.py` that always returned an experiment with no patches, and `analyzer/report_data.py`'s third decode of the same hypothesis and outcome blocks — beside a raw-dict reader in `workspace/reads.py` carrying the same name as the typed one. Production rises by 160 for the single owner and the named reasons the readers now carry, accounted for in the production row below. The remaining 224 lines are in tests, and 238 of them are `tests/data/workspace_reader_parity_snapshot.json`: the golden's three single-generation labels and its epoch-wide label now pin the typed record, which resolves each generation's sibling patch files into the record and states every outcome field the hand-written fixture body omits, so each pinned record is longer than the raw body it replaces. Against that, `tests/test_hydration_enum_coercion.py` loses 63 lines: they pinned where the second read path disagreed with the first, and one decoder leaves no second path to disagree. The rest is coverage for behaviour that did not exist before — 19 lines in `tests/test_workspace_layer.py` for the epoch-wide walk's two dispositions, an absent record and a present one that will not parse; 13 in `tests/test_dashboard_server.py` where the epoch feed's fixture now references its patch through `patch_ids` as the writer does, which is what the served patch set is drawn from; 8 in `tests/test_epoch_journal.py` turning the inline-patches acceptance case into the refusal case and stating why a silently empty patch set is the danger; 7 in `tests/_workspace_reader_parity_harness.py` for the second pinned list and the drift it catches; and 2 in `tests/test_generation_ordering.py`. |
| One decoder for the generation record (production) | 202,668 | +160 | 202,828 | Issue #411: `epoch/journal.py` rises by 222 and is more than the whole of the increase. It gains the record's two encoders, `experiment_body` and `patch_body`; the one acceptance test for a stored body, which settles whether the record is there, whether it is a JSON object this format version reads, and whether it is the one shape the record has; the typed readers `read_experiment_if_present` and the two epoch-wide walks over the shared enumeration; the body readers `read_experiment_body` and `read_epoch_experiment_bodies` that the JSON views take; and `ExperimentRecordError` with the refusals that name the record and its storage key — a body that is not a JSON object, a body inlining a `patches` array, a non-list `patch_ids`, a referenced patch file that is missing or will not parse, and a hypothesis or outcome block that does not decode. Set against it, `epoch/analysis.py` falls by 110 as the lossy hydrator, its two nested hydrators, the file walk that fed them and the pair-returning view builder are deleted, leaving the generation-list builder and the note that states which records the document leaves out; `workspace/reads.py` falls by 25 as the raw-dict `read_experiment`/`read_experiments` pair goes while its enumeration stays; `analyzer/report_data.py` falls by 11 as its own patch resolution and its two movement coercions become projections off the typed record; and `dashboard/filetree.py` falls by 8 as its private patch encoder becomes the record's own. The remainder is what degrading at a boundary costs: 18 lines in `query/lineage_view.py`, 17 in `query/epoch_view.py`, 16 in `query/events_index.py` and 10 in `query/hypothesis_view.py` for the strict decode caught once per view assembly and the conditional key that carries the reason, which an intact workspace never emits; 12 in `analyzer/report_sections.py` for the report masthead line that says how many records were excluded; 11 in `epoch/_storage.py` for the `RecordError` base that lets a view catch one type; 6 in `epoch/__init__.py` for the re-exports; and one line each in `evolve/round_reporting.py`, `index/ingest.py`, `proposer/scorecard.py` and `runtime/resume.py` for the refusal added to an existing except arm, against 2 recovered in `evolve/loop.py` where a hand-read round index becomes a field. |
| One decoder for the generation record (production logic) | 112,977 | +16 | 112,993 | Issue #411: the executable growth is what one owner costs where four readers each carried a fragment. `epoch/journal.py` adds the encoder bodies, the shared acceptance test, the two epoch-wide walks over one enumeration, and eight `raise` statements that each name a record and a storage key; four query views add the boundary catch and the conditional reason key; and the analyzer's report gains the field and the masthead branch that state which generations were left out. Deleted against that are the lossy hydrator and its two nested hydrators in `epoch/analysis.py`, the raw-dict reader pair in `workspace/reads.py`, `analyzer/report_data.py`'s patch resolution and movement coercions, and `dashboard/filetree.py`'s private patch encoder. The two counts nearly cancel; what remains is the executable half of stating, per view, what an unreadable record means there. |
| The contract draft is a library package of its own (total) | 464,932 | +22 | 464,954 | Issue #436: the editable model of an evaluation contract and the single implementation of each edit that can be applied to one lived inside the driver that serves them over HTTP, so `zicato.reflection` had to import a driver to stage a finding's proposed edit and could not join the source list of the contract forbidding that. Both modules move to `zicato.contract_draft` with no change to what any operation does. `contract_draft/__init__.py` is new, 21 lines, all of them the docstring naming what the package models, which callers drive it, and the fact that nothing in it serves a request or calls a model; it exports nothing, because every consumer imports the submodule that defines what it uses. Against that `builder/__init__.py` falls by 9, the three re-exported draft names and the blank line above them. The two moved modules rise by 3 between them where their docstrings stop naming the builder form and its copilot as their only callers and name the reflection adjudicator beside them, `reflection/__init__.py` rises by 1 naming the layer its lazy imports pull, and `reflection/apply.py` falls by 1 where the paragraph explaining that the module exists so the CLI need not import the builder is replaced by the shorter statement that neither reaches a driver at all. `pyproject.toml` carries 6: `zicato.contract_draft` joins the source list of the drivers contract and the forbidden lists of the two cuts declared inside the library, `zicato.reflection` joins that same source list in this change so the contract is never red at any commit, and two comment lines name the new package where they enumerate what sits above the modelling and execution layer. `tests/test_knob_registry.py` carries the last line, its one builder import split into a builder import and a contract-draft import. Production logic falls by 8 and its limit ratchets to 112,985, so this change records no row for it. |
| The contract draft is a library package of its own (production) | 202,828 | +15 | 202,843 | Issue #436: `contract_draft/__init__.py` is new, 21 lines of docstring naming what the package models and stating that nothing in it serves a request or calls a model; it exports nothing, so a consumer imports the submodule that defines what it uses. Against that `builder/__init__.py` falls by 9 — the three re-exported draft names and the blank line above them — and `reflection/apply.py` by 1, where the paragraph explaining that the module exists so the CLI need not import the builder is replaced by the shorter statement that the draft and its operations are library code neither it nor the CLI reaches a driver for. The moved `draft.py` and `operations.py` rise by 3 between them where their docstrings name the reflection adjudicator beside the builder form and its copilot as callers of the same operations, and `reflection/__init__.py` by 1 naming the layer its lazy imports pull. The six lines added to `pyproject.toml` reach the total alone, because it sits outside the runtime package. Production logic falls by 8 and its limit ratchets to 112,985: the removed re-export is the change's only executable line, and nothing executable replaces it. |
| Typeface options named after their faces (total) | 464,954 | +37 | 464,991 | Issue #448: the twelve typeface options were identified by a mode initial and a position in a selection exercise that is absent from the repository, so `T14` says nothing about the faces it applies. Each option now carries the face it sets headings in as a kebab-case id, the form the colour themes beside it already use, and the picker row drops its leading prefix. `src/zicato/dashboard/static/js/ui.js` carries 11 of the rise, accounted for in the production row below. The remaining 26 are in the console behaviour suite: `test/variant_t_candidate.test.mjs` gains 27 for a new case pinning that every id retired by this change still resolves to the faces it selected — on a passed value, on the persisted read path, and through `applyTypeface` — plus the id-roster assertion growing to three wrapped lines and the two shape checks that each id is kebab-case and each label carries the face names alone; `test/settings.test.mjs` loses one line, a parenthetical about which id form the assertion uses that the named ids make redundant. `src/zicato/dashboard/static/css/console.css` and the other two suite files rewrite their ids in place and do not move. `docs/design/typeface-study/README.md` gains six lines stating that its positional labels index that record's own candidate grid and naming the ids the dashboard uses instead; it is Markdown and reaches no count. |
| Typeface options named after their faces (production) | 202,843 | +11 | 202,854 | Issue #448: the whole of the rise is `src/zicato/dashboard/static/js/ui.js`, and five of the eleven lines are executable, accounted for in the logic row below. The other six are comment. The `TYPE_OPTIONS` header gains the rule an id follows — the id names the face the option sets headings in, and a pairing with a body or code face from another family is spelled out in the label rather than the id — which is what makes `archivo-narrow` and `barlow-condensed` legible as the two options that pair a display face with Space Grotesk. `LEGACY_TYPE_MAP`'s comment gains the second kind of value it now holds and states that the map exists to retire both kinds, so nothing else may spell them. Against those, the note on `DEFAULT_TYPE` and the one on `TYPE_THEMES` each lose a line of text that described how the constant came to be rather than what it is. The stylesheet's twelve `[data-t-type]` blocks, their comments and the option array all rewrite their ids in place and cost nothing. |
| Typeface options named after their faces (production logic) | 112,985 | +5 | 112,990 | Issue #448: the executable growth is `LEGACY_TYPE_MAP` alone, which goes from one line holding the three mode ids to six holding those plus the twelve retired option ids, so a typeface persisted under `zicato.T.typeface` before the rename still resolves to the faces it selected. The twelve option entries and `DEFAULT_TYPE` are rewritten in place and cost nothing. |
| One reader for a recorded tournament decision (total) | 464,991 | +176 | 465,167 | Issue #411: two modules knew independently how a tournament decision is spelled on disk and disagreed, so a record written as `{"outcome": {"decision": "promoted"}}` was a promotion to the classifier the dashboard serves and a rejection to the typed record rebuilt from the same bytes. `tests/test_epoch_journal.py` carries 100 of the rise: a parametrized case reading a promotion back through the typed record from each of the three non-canonical spellings, a case asserting the classifier and the typed record resolve one body to one decision, a case pinning that an outcome naming no decision still reads back as rejected, and the two helpers that write a minimal stored body. Every one of them fails against the code as it stood, except the last, which pins the policy the fix makes explicit. `tests/test_hydration_enum_coercion.py` carries 4 more, where the pin on a structurally wrong decision value gains the sentences stating why it now reads back as rejected rather than carrying a number or a container into a field the dataclass types as a decision. Production carries the remaining 72. |
| One reader for a recorded tournament decision (production) | 202,854 | +72 | 202,926 | Issue #411: `core/tournament.py` gains 51 lines for `recorded_decision_token` and the `OUTCOME_DECISION_KEYS` precedence it walks, roughly four fifths of it prose stating which writer emits which spelling and why the precedence only decides hand-edited records. `epoch/journal.py` gains 24: the import, the resolved token, a docstring on the outcome decoder stating that a bare-string outcome names the decision and nothing else, and the comment at the one site that now chooses rejection for a record naming no decision — a choice the field's non-optional declaration forces and issue #446 measures the cost of removing. `query/decisions.py` falls by 3, its eleven-line walk over the four shapes becoming one call. |
| One reader for a recorded tournament decision (production logic) | 112,990 | +6 | 112,996 | Issue #411: the executable growth is 10 lines in `core/tournament.py` for the key tuple, the two shape tests and the key walk, and 5 in `epoch/journal.py` for the import, the resolved token, the normalization of a bare-string outcome to an empty field mapping, and the two lines the decision expression now spans. Against that `query/decisions.py` drops 9, its whole walk replaced by the shared call. |
| The console is named for what it is (total) | 465,167 | +80 | 465,247 | Issue #449: the console carried the letter it won a dashboard bake-off under, and `data-variant="T"` — the attribute the 54 theme, typeface and layout selectors in `css/console.css` all tested against — could only ever hold one value, because nothing selects a variant. The attribute and the letter are both deleted: the mount point is `#console-root`, the entry is `console.js`, the ten `variant_t_*.test.mjs` files take the name of the surface each exercises, and `docs/design/variant-T.md` becomes `docs/design/CONSOLE-CHANGELOG.md`. Dropping a term from a selector shortens a line without deleting it, so the count rises rather than falls: 80 lines, of which 40 are the case pinning the preference migration in `test/settings.test.mjs` and the two comment blocks that separate it from the typeface-id migration `test/candidate_surfaces.test.mjs` pins, and the rest is the preference store accounted for below. |
| The console is named for what it is (production) | 202,926 | +40 | 202,966 | Issue #449: `js/core/prefs.js` is new and carries the whole of the rise. It owns the `zicato.console.<name>` key namespace, the two guarded accessors that survive a browser with site data blocked, and the fallback that reads a value stored under the retired `zicato.T.<name>` spelling when the current key is absent, so a browser carrying preferences written before the rename keeps its viewer's theme, typeface, page scale, text size, rail width and builder chat geometry. Over half the module is comment: that the fallback exists to retire one key spelling and what to delete when it can go, and that a stored value's own spelling is a separate question `ui.js`'s `LEGACY_TYPE_MAP` answers for the typeface. Against it, `js/ui.js` and `js/builder/model.js` both shed their own `localStorage` access, and `index.html` sheds the mount element's unstyled `variant-root` class. |
| The console is named for what it is (production logic) | 112,995 | +7 | 113,002 | Issue #449: the executable growth is the two accessors in `js/core/prefs.js` and the constants holding the two key prefixes they read, less the two raw-storage helpers deleted from `js/ui.js` and the four deleted from `js/builder/model.js`, and less the `data-variant` stamp deleted from `js/shell.js`. The selector rewrite in `css/console.css` costs nothing: each of the 54 rules is shortened in place. |
| A concept atlas for readers new to zicato (total) | 465,247 | +2,810 | 468,057 | Issue #393: the vocabulary reachable from user surfaces is roughly a hundred terms, the two existing glossaries define the exotic ones and omit the load-bearing ones, and neither shows how a term relates to the others. `docs/atlas/index.html` is one self-contained page carrying all 2,810 of the rise: 88 concept definitions read out of the modules that implement them, 144 typed relationships between them, an interactive map of the five parts of a round with a guided walk through the 28 that carry the loop, ten drawn figures for the mechanisms prose describes poorly (the contract's three requirements, the round as a cycle, the gate in order, the board split, champion tenure across rounds, a mutation point being rewritten, the difference plot, the loss-floor descent, the drift-loss grid, and the radar), and the command surface. Roughly a third of the file is the concept data, a third the page's own styles across sixteen themes, and a third the layout and interaction code. Production and production logic do not move: nothing here is imported by the library, and the page has no build step. `docs/atlas/README.md` adds 34 lines that reach no count, because the checker does not measure markdown. The enforced total lands 35 above the measurement, which is slack `origin/main` already carried rather than anything this change introduces. |
| The typed outcome record can say that no decision was recorded (total) | 468,057 | +121 | 468,178 | Issue #446: `OutcomeRecord.tournament_decision` was declared non-optional and its decoder read an outcome naming no decision as a rejection, so an in-flight experiment was counted as a settled loss in every promotion statistic the index serves. The field is now optional, and `None` means no decision was recorded. Tests carry 112 of the rise: `tests/test_index_ingest.py` gains 39 for the case that fails against the old decoder, an outcome with no decision ingesting as a NULL `tournament_decision` that the settled-only readers skip; `tests/test_analyzer_report.py` gains 35 and `tests/test_epoch_analysis_diagrams.py` 20 pinning that the report view and the trajectory table render such an outcome as `pending` rather than the spelling of an absent value; `tests/test_epoch_journal.py` gains 18 for a write-and-read round trip of a record with no decision, alongside the absence pin restated for the new reading. `tests/test_hydration_enum_coercion.py` restates two pins in place. Production carries the remaining 9, accounted for below. |
| The typed outcome record can say that no decision was recorded (production) | 202,966 | +9 | 202,975 | Issue #446: `core/experiment.py` gains 5 of docstring stating what `None` means in the field and that the field keeps no default so every construction site names its decision; `evolve/round_context.py` gains 6, the guard that skips a decisionless record in the genealogy walk and the two comments stating that such a record is still in flight and that the recombination pool admits only rejected outcomes; `index/ingest.py` gains 3 of comment stating that a decisionless outcome writes NULL; `epoch/analysis.py` and `core/tournament.py` gain 2 each for the pending fallback and the shared reader's docstring. Against those `epoch/journal.py` falls by 9: the comment defending the rejection default goes with the default, and the decision expression shrinks from three lines to one. The logic count falls by 2 for the same reason and moves no limit. |
| An endpoint outage is not a rejected proposal (total) | 468,178 | +532 | 468,710 | Pull request #454: a round whose every proposal attempt died on the transport settled as a rejected proposal, which burned a generation id and journalled an experiment stating a cause -- "failed parsing or post-apply validation" -- that never happened. `tests/test_proposer_infra_outage.py` is new and carries 328 of the rise: the classification pinned in both directions over real emitter templates rather than approximations of them, the three content rejections that quote network prose and must keep rejecting, the deferral leaving an empty workspace, and the settlement each classification routes to at both field sizes. `tests/test_epoch_round_integrity.py` adds 45: the two Foe episode templates join the emitter-pinning test, and a new check requires every call-boundary prefix to be found in the tree or declared retired, which is the direction that test could not check and the direction three stale prefixes had drifted in. The remaining 194 are production and are accounted for in the row below. The previous limit stood 35 lines above the measurement, so this row sets the limit to the new measurement and its delta is the 567-line rise less that slack. |
| An endpoint outage is not a rejected proposal (production) | 202,975 | +194 | 203,169 | Pull request #454: `src/zicato/evolve/persist.py` adds 99 -- the round-level rule that a proposal trail made entirely of transport failures measured nothing, and the settlement it routes to, which writes no experiment, no generation and no journal entry, leaves the generation id free for the next attempt, and returns the deferred-infra decision the endpoint-outage circuit already uses so the loop backs off instead of counting a rejection. `src/zicato/evolve/field_candidates.py` adds 60: the whole field's attempt trail is collected across its slots and checked before any field shape settles, because a wider field's rejection also counts toward the consecutive-rejection breaker and also reads back as evidence about the proposer. `src/zicato/epoch/round_integrity.py` adds 35 against 15 deleted: the per-error predicate its marker scan already applied becomes public so both subsystems answer the same question from one vocabulary, the two Foe episode templates join the call-boundary anchor that keeps that vocabulary off text the model or the child snapshot authored, and the prefixes whose emitters are gone are named as retired. |
| An endpoint outage is not a rejected proposal (production logic) | 113,002 | +100 | 113,102 | Pull request #454: the executable growth is 60 lines in `src/zicato/evolve/persist.py` (the outage predicate, and the deferral's warning, round-log emissions, heartbeat and returned outcome), 33 in `src/zicato/evolve/field_candidates.py` (the per-slot walk over the rejections, the early return, and the widened import), and 10 in `src/zicato/epoch/round_integrity.py` (the extracted predicate's signature and body, two call-boundary prefixes, and one marker for the bare OAuth error code google-auth renders with no reason phrase beside it). Nothing executable is removed: the duplicate marker vocabulary this replaces was data rather than logic, and `_matched_markers` keeps its loop over one call. The previous limit stood 3 lines above the measurement, so this row sets the limit to the new measurement and its delta is the 103-line rise less that slack. |
| A first run reachable from the README (total) | 468,710 | +1,054 | 469,764 | Issue #396: five of the seven artifacts a first round needs had to be hand-authored, so the templates a round runs against are now shipped and copied. `example_workspace/` carries 658 of the rise: the proposer 181, the adapter 169, the scoring contract 86, the predicates 75, the two model-role callables 55, the system under test 36, the two package docstrings 28 and 24, and the board 4. Its brief is markdown and reaches no count here. `cli/example_scaffold.py` is 127 and `cli/dotted.py` 45, the latter the click wrapper moved out of `cli/commands/evolve.py`, which falls 43. `tests/test_example_scaffold.py` is 173 and holds the claim: the seven artifacts, the wiring, the never-clobber rule, a board whose four entries separate, and a real three-round loop that reaches the floor. The remaining test files fall 45 net, freed of two options `zicato evolve` does not take; the CLI-HELP golden rises 6 and one example's mock module 5. |
| A first run reachable from the README (production) | 203,169 | +915 | 204,084 | Issue #396: `example_workspace/` is 658 of it and ships in the wheel because the quickstart has to work from an installed package; the modules are kept as source rather than as template strings so the linter and the type checker read them and a reader can open the file the operator receives. `cli/example_scaffold.py` adds 127 for the copy, the never-clobber rule, and the config overlay naming the copies. `cli/commands/init.py` adds 40 for the `--example` option, its help, and the report of what was written; `cli/init_cmd.py` adds 37 threading it through the workspace initializer and writing the example's own scoring contract. `evolve/loop.py` adds 19 and `evolve/round_entry.py` 11 resolving the two model roles from the workspace configuration; `runtime_factory.py` adds 16 for `resolve_role_call_llm`, the single resolution both call. `cli/dotted.py` is 45, `mutation/__init__.py` 5 for the `replacement_source` export, and `cli/commands/evolve.py` falls 43 with the two deleted options and the helper that moved. |
| A first run reachable from the README (production logic) | 113,102 | +397 | 113,499 | Issue #396: the executable growth is 368 in the eleven new files and 29 net in the rest. The example proposer is 96 -- the defect table, the mutation-point lookup, the token edit, the experiment and hypothesis it returns, and the post-apply validation call every proposer owes the round; the adapter 79 -- the policy parse, the note composition, the runnable-generation class and the three adapter methods; the scoring contract 86 and the board 4, which are JSON and carry no comment syntax the counter can discount; the predicates 21; `cli/example_scaffold.py` 52; `cli/dotted.py` 15; the model callables 8 and the three package modules 7. Of the changed files, `cli/commands/init.py` rises 27 for the new option and the report loop, `cli/init_cmd.py` 21, `evolve/loop.py` 7 for the role resolution, `mutation/__init__.py` 5, `runtime_factory.py` 1, and `cli/commands/evolve.py` falls 32. |
| One glossary defines every term a user surface uses (total) | 469,764 | +152 | 469,916 | Issue #393: `tests/test_vocabulary_glossary.py` is new, 144 lines, and pins the glossary's alphabetical order and internal links, the closed list of terms the console and the `--help` text use, and every quick-reference row's link into the glossary. The CLI help golden grows by 3 lines where four reworded docstrings wrap differently, and the four docstrings add 5 between them. The glossary, the developer guide, `SELECTION-THEORY.md`, and the `TOURNAMENT.md` cost section (issue #453) are Markdown and enter no count. |
| One glossary defines every term a user surface uses (production) | 204,084 | +5 | 204,089 | Issue #393: the four click docstrings that are the `--help` text of `zicato init`, `zicato board audit`, `zicato epoch rounds`, and `zicato evolve --mode`, reworded so each explains itself in the glossary's words rather than the implementation's; `epoch.py` gains 2 lines and the other three 1 each. |
| One glossary defines every term a user surface uses (production logic) | 113,499 | +1 | 113,500 | Issue #393: the `--mode` option's help string in `cli/commands/evolve.py` is an executable string literal and grows from 8 lines to 9 when reworded; the other three docstrings are prose and reach no logic count. |
| Six atlas definitions corrected against the source (total) | 469,916 | +2 | 469,918 | Issue #393: six entries in `docs/atlas/index.html` disagreed with the code. Corrected: the trajectory record is the scalar path of the promoted lineage with a plateau flag; a Copeland score counts duels won, a bye included, with no half-points; calibration names both the critic-calibration channel and the A/A noise-floor step; best-of-N chooses by critique and screening only vetoes; the gate applies three rules; the Pareto record keeps the candidates no other candidate dominates. The two added lines are in the figure paragraph that places the trajectory strip in the Traces view. |
| Replicates sized from the measured noise floor (total) | 469,918 | +871 | 470,789 | Issue #465: `tests/test_replicate_sizing.py` is new, 228 lines: the formula's reference values, its inverse recovered at every count on the ladder and refused past the cap, the three-tier resolver, the registry threading, and the round-0 log lines. `tests/test_selection_strategies.py` gains 35 for a racing rung driven with a floor and one without; `tests/test_query_eval_view.py` 24 for the ladder's statistic and replicate source; `tests/data/endpoint_route_snapshot.json` 4 for the two eval-health probes. The production rise is the row below. |
| Replicates sized from the measured noise floor (production) | 204,089 | +576 | 204,665 | Issue #465: `src/zicato/tournament/detectable_effect.py` is new, 198 lines, 82 of them the Student-t quantile moved out of `src/zicato/query/eval_view.py`, which falls by 36 net after its ladder gains the floor statistic and the replicate source and its reader resolves the count in effect. `src/zicato/selection/replicates.py` is new, 212, the three-tier resolver and its record. `src/zicato/evolve/round_prepare.py` carries 70 for the resolver's log lines, `src/zicato/selection/strategies/racing.py` 55 for the gap a rung resolves, `src/zicato/evolve/round_entry.py` 27 for the call site and the heartbeat stamp, `src/zicato/selection/registry.py` 20 for the two `make_strategy` arguments, `src/zicato/runtime/effective_settings.py` 15 for the three tiers and the record key, `src/zicato/dashboard/static/js/views/evals_health.js` 12 for the source labels, and `src/zicato/selection/strategy.py` 3. |
| Replicates sized from the measured noise floor (production logic) | 113,500 | +324 | 113,824 | Issue #465: the executable growth is the inverse's search over the replicate ladder and the resolver's tiers in the two new modules, the racing rung's within-noise loop, the log branches in `round_prepare.py`, the call site in `round_entry.py`, and the ladder's two source fields; docstrings and comments are the balance of the production rise. |
| Three tournament structures behind an experimental opt-in (total) | 470,789 | +512 | 471,301 | Issue #394's namespace begins: `single_elim`, `double_elim` and `swiss` resolve only under the contract's `experimental.tournament_structures` flag, the first member of the `experimental` block. Tests carry 234 of the rise. `tests/test_selection_experimental_structures.py` is 277 lines, 191 of them the single-elimination, double-elimination and Swiss cases moved out of `tests/test_selection_strategies.py`, which falls by the same 191; the rest pin the refusal without the opt-in, the resolution with it, and that the registry's experimental tokens equal the core set. `tests/test_epoch_contract.py` gains 24 for the pair stating that the block at its default keeps the hash and a raised flag moves it; `tests/test_knob_registry.py` 23 for the scaffold pin, the omit-set entry and the container exemption; `tests/test_cli_help.py` 20 for the option's two refusals; `src/zicato/dashboard/static/test/builder.test.mjs` 18 for the Experimental section's toggle and the gated card group; `tests/data/workspace_reader_parity_snapshot.json` 12 where the serialized scoring of four fixture epochs gains the block; `tests/_contract_pins.py` 11 for the helper that opts a test contract in; and thirteen other test files 40 between them for the opt-in at each contract construction site. Outside tests, the three example contracts gain 3 each, the CLI help golden 5 where the option's help grows, and the capture harness 5. Production carries the remaining 259, accounted for below. |
| Three tournament structures behind an experimental opt-in (production) | 204,665 | +259 | 204,924 | Issue #394: `core/scoring_config.py` gains 61 for the `ExperimentalConfig` dataclass, its docstring, the `experimental` field and the load-time refusal of an experimental structure without the flag; `contract_draft/operations.py` 34 for `set_experimental`; `core/tournament.py` 28 for the experimental token set, the key and the one refusal message every surface renders; `cli/commands/evolve.py` 27 for the option's validating callback and its longer help; `selection/registry.py` 26 for the second registry, the gated resolution and the `experimental_structures` parameter, whose keyword-only marker it shares with the replicate parameters the noise-floor sizing added; `builder/copilot_tools.py` 22 for the `set_experimental` tool and the reworded `set_structure` description; `dashboard/static/js/views/builder.js` 21 for the gated card group and the Experimental section; `dashboard/static/js/builder/model.js` 15 for the split structure lists and the predicate; `selection/experimental/__init__.py` 12 of package docstring; `builder/api.py` 4 for the dispatch arm; `runtime/resume.py` 3, `core/types.py` 2 and `selection/__init__.py` 2 for the export and the threaded flag; `evolve/round_entry.py` and `cli/commands/tournament.py` 1 each for the flag passed to `make_strategy`. The three moved strategy modules change no line. |
| Three tournament structures behind an experimental opt-in (production logic) | 113,824 | +130 | 113,954 | Issue #394: the executable growth is 24 lines in `core/scoring_config.py` (the dataclass, the field, the import and the refusal), 23 in `contract_draft/operations.py` (`set_experimental`), 18 in `cli/commands/evolve.py` (the callback and the option's help string), 18 in `dashboard/static/js/views/builder.js`, 10 in `selection/registry.py`, 9 each in `builder/copilot_tools.py` and `core/tournament.py`, 6 in `dashboard/static/js/builder/model.js`, 4 in `builder/api.py`, 3 in `runtime/resume.py`, 2 each in `core/types.py` and `selection/__init__.py`, and 1 each in `evolve/round_entry.py` and `cli/commands/tournament.py`. Nothing executable is removed. |
| Production logic per subsystem and over time (total) | 471,301 | +699 | 472,000 | `tools/line_budget.py` adds 336: the partition of the three measurements by subsystem with each subsystem's prose share, the `--report` table, the per-subsystem table in this document that `--write-summary` writes and `--check-ledger` verifies, and the `--history` walk over first-parent commits through a content-addressed cache. `tests/test_line_budget.py` adds 352: hand counts of one small file per language in a subsystem of its own, the identity that the subsystems sum to the repository-wide counts, the table checks, and the history walk's caching and its refusal of a commit off the first-parent chain. The CI job writes the `--report` table into the job summary (8 lines) and `.gitignore` names the history cache (3). No production file changes, so the production and production-logic counts hold. |
